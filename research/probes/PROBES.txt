Files removed from research/probes. Full sources: git show 1785223:<path>

research/probes/absent.c: Probes how a missing or absent symbol/file is handled in the macOS arm64 APE loader research. Defines a single trivial function f returning 1, serving as a minimal object for loader testing.
research/probes/alive.s: A minimal macOS arm64 probe that writes "ALIVE" to stdout and exits. It uses raw BSD syscalls via svc #0x80 with x16 encoding write (0x2000004) and exit (0x2000001). It confirms basic syscall-based code execution works in the APE loader research.
research/probes/ap.s: An arm64 Darwin assembly probe that prints the value of the 4th main() argument register x3 (argv[3], the apple vector) as a 16-digit hex string terminated by newline. It uses the raw BSD write syscall (class 0x200, number 4) and exit (class 0x200, number 1) via svc 0x80 on a 64-byte stack buffer, formatting nibbles manually without libc. It does not report any result in the source itself.
research/probes/arith.c: arith.c empirically derives the arm64 shared-cache trie address formula by calling syscall 0x2000126 (sysctl) to get the cache base, parsing __TEXT (0x19) and DYLD_EXPORTS_TRIE (0x80000033) load commands for textvm and exoff, then computing trie_mem = (cache - slide) + exoff + slide where slide = img - textvm. It walks the cached exports trie to find "getpid" and compares the result against dlsym truth, verifying trie_unslid = cacheBase_unslid + exoff reaches memory correctly via the image slide.
research/probes/av.c: A macOS arm64 probe whose _main wrapper passes the raw 4th argument (apple vector) to cmain and exits via syscall 0x2000001, while writing output with syscall 0x2000004 using inline x16 svc calls. It dumps the first 16 apple[] pointer values as hex to inspect what the kernel hands a raw Mach-O entry point.
research/probes/baseline.txt: This probe runs an arm64 Mach-O/APE binary at /tmp/apex/probe-arm64.com with args hello world and logs its exe path, cwd, empty env, and darwin/arm64 runtime context (10 CPUs); it tests baseline APE loader startup/exec behavior and shows the process launched successfully with no exec error (exeErr=<nil>).
research/probes/bound.c: This macOS arm64 probe calls syscall 294 (0x2000000|294, shared cache info) via svc 0x80 to get the dyld shared cache base, then uses mach_vm_region with VM_REGION_BASIC_INFO_64 to measure the contiguous cache mapping size and dumps 4-byte fields near offset 0x130. It concludes the cache mapping size from mach_vm_region should be used for scanning instead of the header's mapping table. It also prints subCacheArrayCount/cacheType fields near 0x130.
research/probes/c0.c: A macOS arm64 probe that issues svc #0x80 with raw syscall class/number pairs 0x0, 0x1000003, 0x2000014, 0x3000000, and 0x4000000 to see which syscall classes return normally. Each test lives in its own function so a hung or fatal class can be identified, and it writes progress with write(2) (x16=0x2000004), exiting via x16=0x2000001 after go().
research/probes/cache.c: This probe locates libSystem by content inside the dyld shared cache using raw SVC syscalls only, finding it via the shared_region_check_np syscall (0x2000000|294) then scanning the cache image array at header offsets 0x18/0x1C or 0xE0/0xE4. It walks each image's export trie via LC_DYLD_EXPORTS_TRIE (0x80000033) or LC_DYLD_INFO(_ONLY) (0x22/0x80000022) to check for mmap, pthread_create, and getentropy exports, proving the loader survives library renaming or relocation.
research/probes/cchdr.c: This probe dlopens libSystem.B.dylib and calls the private shared_region_check_np syscall (BSD class 0x2000000 | 294, via svc #0x80 with x16) to get the dyld shared cache base, then dumps the first 32 raw 32-bit words of the cache header starting at the magic string. It was written to empirically identify the real imagesOffset and imagesCount fields by inspecting header offsets word-by-word.
research/probes/cls.c: A macOS arm64 probe that issues raw svc #0x80 syscalls with fixed x16 numbers 0x1, 0x400, 0x1000000, 0x2000000, and 0x0 (via wrapper svc(), write() on fd 2, and hex printer), printing each return value and exiting via x16=0x2000001 (exit) with status 0. It tests which large or invalid BSD syscall numbers survive versus kill the process. It expects the raw 0x0 SVC (t0, run last) to die, and prints "ALL DONE" only if all earlier ones returned.
research/probes/cls2.c: A macOS arm64 probe that calls class-0 legacy syscalls via x16 (svc #0x80): fork (#2), write (#4), invalid #0x60, and indirect #0 which takes the number from x0. It prints each returned value in hex, then exits using x16=0x2000001. No observed results are recorded in the file.
research/probes/cnt.c: This probe calls sysctl (x16=0x2000000|294, i.e. mib={CTL_KERN,KERN_PROC,...} style) to fetch the current process's dyld image info, then reads the header fields at offsets 0x1c0 (imagesOffset) and 0x1c4 (imagesCount) plus the unslid base at 0xe0 to compute the ASLR slide. It walks the imagesCount array entries (32 bytes each) applying the slide and validates each pointed-to Mach-O by magic 0xfeedfacf and filetype 6 (MH_DYLIB) or 8 (MH_BUNDLE), counting zero-addr, bad-magic, and non-dylib entries and printing the array's file-offset span.
research/probes/cp.c: This probe reads dyld_all_image_infos via _dyld_get_all_image_infos and scans the commpage at 0x0000000FFFFFC000 over offsets 0x5800..0x6800 in 8-byte steps to find a pointer matching that struct. It tests whether dyld stashes the all-image-infos pointer in the arm64 commpage so an APE loader could locate it without dyld APIs. The file does not state the result, only printing a MATCH offset or "no match".
research/probes/ctl.c: Tiny probe that calls _exit(42) to test syscall 1 behavior. No offsets, load commands, or flags are involved.
research/probes/d.s: d.s is a trivial macOS arm64 probe whose _main just branches to _exit. It tests the minimal ARM64 calling convention path, calling exit with whatever x0 already holds rather than loading a syscall number or setting a flag. It shows nothing further; no syscall numbers, offsets, load commands, or results are recorded.
research/probes/dbg.c: This probe uses syscall 294 (SYS_shared_region_check_np) with x16=0x2000000 to obtain the shared cache base, then parses the Mach-O header at hardcoded 0x188bb0000 (libsystem_c.dylib) to dump ncmds and each load command by type, size, and offset fields. It identifies LC_SEGMENT_64, LC_SYMTAB, LC_DYLD_INFO, LC_DYLD_EXPORTS_TRIE, and LC_DYLD_CHAINED_FIXUPS, printing export trie and segment fileoff details. The file reports no observed result beyond the printfs.
research/probes/dbg2.c: dbg2.c scans a file at 0x10000 steps for a Mach-O arm64 payload (magic 0x464c457f, cputype 183), prints its entry, phoff, phentsize, phnum plus each LOAD segment's vaddr/offset/filesz/memsz/flags, computes the load range, and checks whether vaddr%16k equals offset%16k per segment. It then test-mmaps the page-aligned base over the span with MAP_FIXED|MAP_ANON to see if the target address is free. It only prints findings like "payload at", "no payload", or mmap OK/FAILED; no overall conclusion is stated.
research/probes/dbgi.c: This probe calls syscall 0x2000126 (sysctl) to locate the shared cache, then walks its image list at header offset 0x1c0 to find Mach-O images with LC_SYMTAB (0x2) and LC_SEGMENT_64 (0x19) for __TEXT/__LINKEDIT. It computes the ASLR slide from regionStart at offset 0xe0 and derives symtab/strtab addresses to verify they are readable, printing the first symbol strings for up to four images.
research/probes/dl2.sh: This probe script patches the 24-byte path field at offset 652 in a woven APE binary wl, re-signs it ad-hoc, and runs it to test how the loader resolves the dyld path. It tries real dyld, existing non-dyld files, and a nonexistent path, printing each exit code and first output line. The file does not state a conclusion.
research/probes/dl3.sh: This script patches a 32-byte path field at file offset 652 in a Mach-O binary (apex/wl copied to dt3), re-signs it ad-hoc, and runs it to test whether a custom dyld path is used. It tests two values: a copy of dyld at /tmp/apex/dyld_copy and the canonical /usr/lib/dyld, reporting exit code and first output line.
research/probes/dltest.sh: This probe rewrites the 16-byte name field of the LC_LOAD_DYLINKER command at offset 640+12 in a Mach-O binary (wl), re-signs it ad-hoc, and runs it to see which dyld paths the macOS arm64 APE loader accepts, printing rc and first output line. It tests exact "/usr/lib/dyld" versus mutated variants (trailing char, case change, trailing slash, relative, empty). The file records no results, only the test harness.
research/probes/dump.s: A macOS arm64 probe that dumps the raw stack contents (argc/argv region) via the BSD syscall interface using syscall numbers 4 (write) and 1 (exit) encoded in x16 with svc #0x80, writing 64 bytes from sp to stdout on entry.
research/probes/ec.c: This C probe uses raw arm64 `svc #0x80` syscalls to test XNU syscall return and error signaling: write (0x2000004) to fd 1 and bad fd 999, plus getpid (0x2000014), capturing x0 and NZCV after each call. It prints hex return values and the carry flag, checking whether success/error is distinguished via carry. It demonstrates the classic BSD syscall convention where the carry flag marks errors, with success returning in x0 and error returning an errno with carry set.
research/probes/esr.s: This probe tests whether the APE emulator traps on an invalid syscall number (0x2000123) by writing "start", invoking the bogus call, then writing "bogus syscall RETURNED" via mach_write (syscall 0x2000004) and exiting via mach_exit (0x2000001) if it returns. Its key mechanism is syscall dispatch on the high-bit 0x200 prefix plus invalid low number. It showed that the bogus syscall does not trap, since the file expects the message to print.
research/probes/esr2.c: This probe issues an out-of-range arm64 syscall 0x2000123 via svc #0x80 to observe kernel error handling. It showed the bogus syscall returned -1 with errno set, confirming unknown syscalls fail cleanly rather than crashing.
research/probes/exit0.s: exit0.s is an arm64 macOS probe that tests a raw AArch64 Mach-O syscall exit path. It builds syscall number 0x2000001 (exit) in x16, puts status 42 in x0, and issues svc #0x80. It documents no result.
research/probes/fake.c: A tiny C probe defining fake_export returning 7 for macOS arm64 APE loader research. It provides a synthetic exported symbol to test symbol table and export lookup handling in the loader. It contains no syscalls, offsets, load commands, or reported results.
research/probes/final.c: A zero-import APE loader probe with an LC_MAIN entry that declares libSystem but uses only raw macOS arm64 syscalls via inline svc #0x80, notably write (0x2000004) to fd 2, printing confirmation and whether argc>1; it is a loader-shape test rather than reporting a measured result.
research/probes/find.c: This probe calls task_info(TASK_DYLD_INFO) to get dyld's all_image_info_addr, then scans the read-only comm page at 0xFFFFFC000 in 8-byte steps looking for that pointer to locate dyld_all_image_infos via commpage offsets, and prints the target's version and infoArrayCount; it tests whether the comm page can serve as a fixed anchor for APE loader initialization.
research/probes/fix_kagi.py: Edits ~/.claude.json (backed up first) to replace the Kagi MCP server's KAGI_API_KEY with a key read from /tmp/apex/kagi_key.txt and to redirect uvx cache/tool dirs to /tmp to avoid the sandboxed ~/.cache/uv; it only prints old/new key prefixes and the updated entry.
research/probes/fixed.c: This probe tests whether MAP_FIXED mmap at various candidate payload link addresses (0x100000000, 0x40000000000, etc.) fails due to occupancy or policy, using raw Mach syscalls 0x2000000|197 (mmap) and 0x2000004 (write) via svc #0x80, with flags ANON|FIXED=0x1012 and a follow-up FIXED-without-ANON mapping to check for EBADF legality.
research/probes/go.c: This probe issues an arbitrary Darwin ARM64 syscall number given as argv[1] via svc #0x80 with x0=0 and prints the raw return value, using direct write syscall 0x2000004 to stdout. It is a minimal freestanding APE-style harness for probing syscall behavior with no libc or dyld. The file does not state any results, only the probe and its output format.
research/probes/go.s: A macOS arm64 assembly probe that reads argc/argv from the stack at _start, calls a _go routine with them, then exits via svc 0x80 with x16 set to 0x2000001 (exit syscall on arm64). It tests the raw process entry and exit-syscall convention for an APE loader.
research/probes/gotest/go.mod: A minimal Go module declaration for the gotest probe, naming the module gotest and requiring Go 1.21; it tests nothing itself and reveals no syscalls, offsets, or load commands.
research/probes/gotest/main.go: A trivial Go probe that prints a static-OK marker and its PID to confirm a Go binary runs under the macOS arm64 APE loader. It uses no syscalls, offsets, load commands, or flags beyond os.Getpid(). It just demonstrates the loader's static Go execution path works.
research/probes/hdr.c: A macOS arm64 probe that reads the dyld shared cache header via syscall 294 (SYS_shared_region_check_np, number 0x2000000|294) pointing x0 at a uint64 output, then prints the cache base, magic, and scans offsets 0x00-0x200 in 8-byte little-endian words, tagging any value equal to 0x1ff05c000 as the __LINKEDIT vmaddr and values in the 4GB-1TB range as addresses. It is a reconnaissance tool to locate __LINKEDIT vmaddr within the shared cache header for subsequent probing; the file itself reports no results.
research/probes/hello.s: A minimal macOS arm64 assembly probe that invokes write (0x2000004) to stdout then exit (0x2000001) via svc 0x80, printing hello. It tests basic BSD syscall calling convention on arm64.
research/probes/imgs.c: A probe that returns dyld's loaded Mach-O image count as its exit code by calling _dyld_image_count(), used to inspect how many images a macOS arm64 APE loader has mapped. It relies on the dyld API rather than raw syscalls or load-command parsing. The file records no observed result.
research/probes/imgs2.c: A probe that calls syscall 294 (shared_region_check_np) via x0=&cache to get the dyld shared cache base pointer, then scans four candidate image-info array offset/count pairs (0x18/0x1C, 0x28/0x2C, 0x150/0x154, 0x160/0x164), reading each entry's 64-bit addr and 32-bit pathOff at entry offset +24. It validates candidates by requiring the offset in [0x1000,0x100000], count in [10,20000], addr in [0x180000000,0x300000000], pathOff <0x80000000, and prints entry0's path for manual inspection.
research/probes/jit.c: A zero-import arm64 probe tests whether raw syscalls can mmap RW anonymous memory, mprotect it to RX, and jump to emitted mov w0,#42;ret code, using svc #0x80 with mmap (0x2000000|197), mprotect (0x2000000|74), write (0x2000004), PROT_RW=3/PROT_RX=5, MAP_PRIVATE_ANON=0x1002, and MAP_JIT=0x800 (unused). It prints the returned value and reports JUMP OK if 42, showing RW->RX W^X is permitted for zero-import code, without flushing icache via libSystem.
research/probes/jit2.c: This probe mmap-stresses the APE loader mechanic via raw arm64 syscalls (0x2000000|197 mmap, |74 mprotect, 0x2000004 write): it tests single-step RWX anon mappings, MAP_FIXED (0x10) at link address 0x100000000, a 64MB anon mapping with mprotect RX, and MAP_JIT (0x800). It likely shows RWX is rejected on arm64 while MAP_FIXED lands exactly, 64MB maps/mprotects OK, and MAP_JIT may be accepted.
research/probes/jit3.c: jit3.c probes macOS arm64 raw syscalls via svc #0x80 with x16 holding the syscall number, using mmap (0x2000000|197) and mprotect (0x2000000|74) to test RWX anonymous mappings, W^X enforcement, and MAP_FIXED (0x10) at 0x100000000, distinguishing errors via nzcv carry flag (bit 29) since Darwin returns errno in x0 with carry set.
research/probes/l1.s: l1.s is a minimal arm64 probe that loads syscall number 0x2000001 (__exit) into x16 and exits with status 42 via svc #0x80, exported as both _start and _main.
research/probes/lc.c: A minimal Mach-O load-command editor probe supporting list, del, setcmd, and addhex modes, reading ncmds at offset 16 and sizeofcmds at 20 and walking commands from offset 32 by each cmd's size field. It recognizes command IDs like 0x1 LC_SEGMENT_64, 0xc LC_LOAD_DYLIB, 0x29 LC_CODE_SIGNATURE, 0x80000028 LC_MAIN, and 0x80000034/0x80000035 (DYLD exports trie / chained fixups). It edits headers and command bytes in place but reports no empirical results.
research/probes/locate.c: A macOS arm64 research probe that locates where a dylib's export trie actually lives by using the shared cache address from syscall 294 (0x2000000|294) plus dlsym/dladdr to get the true symbol address, then parsing Mach-O load commands (LC_SEGMENT_64 0x19 for __TEXT/__LINKEDIT vmaddrs and file offsets, LC_DYLD_EXPORTS_TRIE 0x80000033 for the trie offset), and finally scanning memory around the resolved getpid address for the "getpid" trie edge string. It reports cache, image base, text/ledit vmaddrs, exoff, and offsets of trie-name hits relative to the true address to derive the correct slide. The file does not state an explicit conclusion beyond printing hits and comparing cache+exoff / img+exoff against the true address.
research/probes/logpid: The file research/probes/logpid is a single-line output artifact containing PID 41747, likely captured from a logging probe in a macOS arm64 APE loader study. It has no syscall numbers, offsets, load commands, or flags, and reports no findings beyond that PID.
research/probes/logstream.txt: A probe that attempted to run a command, likely a log stream, and failed because the process was sandboxed. The error message shows the attempt was blocked, indicating the probe requires unsandboxed execution to run.
research/probes/m3.c: Probes LC_MAIN entry conventions on macOS arm64 by using raw svc #0x80 write syscall 0x2000004 to print argc, up to six argv entries, and envc without libc.
research/probes/mach.c: mach.c probes which raw SVC syscall classes work on macOS arm64 by running each test in a separate process using BSD syscalls (0x2000000|n, e.g. write=4, getpid=20) and Mach traps (0x1000000|n, traps 3, 27, 31, 28, 2 with x0-x3 args). It reports results via raw write(2) and exits with BSD exit (0x2000001).
research/probes/map.c: This probe reads the dyld shared cache mapping table (mappingOffset at 0x10, mappingCount at 0x14) via syscall 294 (0x2000000|294) to get file-offset-to-vmaddr translations, then resolves the "getpid" symbol using the export trie. It parses Mach-O load commands (LC_SEGMENT_64 type 0x19 for __TEXT/__LINKEDIT vmaddrs and fileOffset, LC_DYLD_EXPORTS_TRIE 0x80000033 for exoff) and tests two hypotheses for the trie's unslid vmaddr: A = 0x180000000 + exoff, B = leditvm + (exoff - leditfo), comparing the resolved address against dlsym's truth.
research/probes/mt.c: This probe calls the macOS arm64 Mach trap syscall via svc #0x80 with x16 set to 0x1000000|trapnum, passing the trap number from argv and zeros in x0-x3, then prints the returned x0. It tests whether Mach traps can be invoked from an APE-loaded binary by exercising the trap dispatch path directly.
research/probes/offset.pl: offset.pl parses Apple's dyld_cache_format.h to compute struct field offsets for dyld_cache_header by summing declared field sizes in order, watching for named fields like mappingOffset and imagesOffsetOld. It handles sized types, alignment, bitfields, and array suffixes to avoid guessing layouts.
research/probes/p.c: A freestanding arm64 probe that takes a hex syscall number and optional x0 from argv and invokes it via raw svc #0x80 with x16 set to that number, printing the return value or dying on SIGSYS. Its own output uses syscall 0x2000004 (write) and it exits via 0x2000001, with a custom _start reading argc/argv from the stack. It is used to map which x16 values raise SIGSYS, but the file records no results.
research/probes/pages.c: This probe reads 16 pages starting at address 0xFFFFC0000 in 0x400 strides to test readability of a fixed VA range, a presumed Mach-O __PAGEZERO or high-address boundary region.
research/probes/patch.c: Patches a Mach-O header's 32-bit flags field at file offset 0x18 in place with a value given as hex on the command line, after reading and printing the magic and original flags.
research/probes/pipetest.c: pipetest.c verifies BSD pipe syscalls work under raw SVC on arm64 by calling pipe via SYS_pipe (BSD 42) twice to obtain two fds, then write (BSD 4) and read (BSD 3) through them, closing with BSD 6. It uses inline asm svc #0x80 with x16 as the syscall number and BSD numbers ORed with 0x2000000, and reports "PIPE WORKS" when the round-trip read yields 2 bytes starting with 'h'. The final print line indicates it computes the result at runtime.
research/probes/probe.c: probe.c scans the macOS arm64 comm page at base 0x0000000FFFFFC000, printing its 4-byte signature and stepping offsets 0x5F00 to 0x6200 by 8 to locate a valid dyld_all_image_infos struct (version 13-40, count 1-3000) by validating pointer ranges. Its key mechanism is direct memory reads with no imports, using the comm page offset and heuristic sanity checks on ver/cnt/image-array/image-path pointers to find libSystem-style image info. The file prints candidates including offset, aii pointer, version, image count, and first image path, but states no confirmed result.
research/probes/probe2.c: Probe2 lists loaded Mach-O images via _dyld_image_count and _dyld_get_image_header/name, then tests RTLD_DEFAULT and dlopen of libSystem.B.dylib with RTLD_LAZY and dlsym for getpid. It checks whether default-symbol and explicit libSystem symbol lookup work under the probe environment, printing each image index, header pointer, name, and resolved symbol addresses; the file records no conclusions.
research/probes/probebuild/go.mod: A minimal Go module file for the probe build probe in a macOS arm64 APE loader research project; it declares module probe and requires Go 1.27. It only sets up the module path and language version, with no dependencies, syscall numbers, offsets, load commands, or flags. It shows no results.
research/probes/probebuild/main.go: A Go probe binary that prints its args, executable path, cwd, APE_PROBE_ENV value, GOOS/GOARCH/ncpu, and exits 3 when argv[1] is "fail"; CI greps its output to verify what a loader passed to the process.
research/probes/probeentry.c: A macOS arm64 research probe that scans a file at 0x10000-aligned offsets for an ELF64 header (magic 0x464c457f, e_machine 183), reads its program headers, and manually maps each PT_LOAD segment with mmap/MAP_FIXED at vaddr page-aligned to 0x4000. It converts p_flags to mprotect protections, preads filesz bytes to each vaddr, and prints each segment's vaddr/memsz/prot plus the entry point's first 16 bytes to verify the payload's real first instructions are actually mapped at that address.
research/probes/pt2.c: This probe tests the raw BSD pipe syscall (42, via 0x2000000|42 and svc 0x80) to determine its calling convention on macOS arm64, checking whether fds are written in-place to an array passed in x0 or via separate out-pointers. It ran the array-in-x0 case in a forked child with a 2s wait and also called a two-pointer variant. It concluded the kernel does not write the fd array in x0, so libSystem pipe via the dyld cache is the reliable approach.
research/probes/rawsys.c: A probe testing raw BSD syscalls from a normally-linked arm64 binary by issuing svc #0x80 with x16 set to the class-1 unix syscall number (0x2000000 base), calling write (0x2000004) and getpid (0x2000014). It verifies the raw-syscall calling convention (x0-x2 args, x16 selector, return in x0) and prints results, though the file shows no captured output. It also notes an untested raw mmap (class1 #197) idea, likely for the APE loader's syscall path.
research/probes/region.c: This probe calls mach_vm_region on address 0x0FFFFC000 to check its protection, then scans 0x4000 bytes there reading 8-byte words under SIGBUS/SIGSEGV guards and prints plausible pointer-like values. The fixed address hints at an ARM64e/APE commpage or shared-page region worth checking near the 4GB boundary. The file records no results, only the mechanism and printfs.
research/probes/region2.c: region2.c probes the fixed macOS arm64 commpage base 0xFFFFFC000 by reading its "comm signature" and scanning offsets 0..0x4000 in 8-byte steps, guarding faults with SIGBUS/SIGSEGV sigsetjmp handlers, and printing any 8-byte value that looks like a valid aligned pointer in 4GB..0xFFFF00000000. It stops the scan at the first faulting offset. The file does not record any observed results.
research/probes/resolver.c: resolver.c probes zero-import libSystem symbol resolution for a macOS arm64 APE loader: it finds the dyld shared cache via raw BSD syscall 294 (x16 = 0x2000000|294, svc #0x80), page-scans it at 0x4000 granularity for MH_MAGIC_64/MH_DYLIB headers, and resolves names through each image's own LC_SYMTAB using __TEXT/__LINKEDIT vmaddrs plus a computed slide. It identifies images by content (does the image export a probe symbol like getpid?) rather than path, and resolves a list of symbols (getpid, mmap, mprotect, pthread_create, getentropy, sysctl, sysctlbyname, dlsym, exit), printing each resolved address and verifying against dlsym ground truth. The file states resolution was verified against dlsym ground truth.
research/probes/scan.c: This probe scans the 16 KB comm page at 0x0000000FFFFFC000 on macOS arm64 for a valid dyld_all_image_infos pointer, as part of APE loader research. It validates candidates by checking pointer alignment/range plus the version (10-40) and image count (1-4000) fields, then verifies the image array and first image/path pointers. It prints the comm page signature and any candidate offsets but reports no hardcoded outcome.
research/probes/scan2.c: scan2.c locates libSystem inside the dyld shared cache without parsing the cache header, page-scanning a 256MB window on 0x4000 boundaries for MH_MAGIC_64 MH_DYLIB (6) Mach-O headers and reading LC_ID_DYLIB (0x0d) to identify each image. For matches it resolves getpid/pthread_create/mmap by walking the LC_DYLD_EXPORTS_TRIE (0x80000033) or LC_DYLD_INFO export trie, translating file offsets via the LC_SEGMENT_64 (0x19) __LINKEDIT vmaddr-fileoff delta. It uses only raw svc #0x80 syscalls (write 0x2000004, shared cache base 0x2000000|294) with no imports, and the file does not state its observed results.
research/probes/seg.c: This probe dumps the Mach-O header and LC_SEGMENT_64 (cmd 0x19) load commands of the main executable via dladdr, reading magic at 0, ncmds at 16, sizeofcmds at 20, and per-segment vmaddr/vmsize/fileoff/filesize/nsects at offsets 24/32/40/48/64. It corroborates the macOS arm64 APE loader premise that the real on-disk Mach-O (base from dladdr) carries the segment layout the loader must respect. It only prints the header and first 10 commands with no stated result.
research/probes/sigsys.c: A macOS arm64 SIGSYS probe that installs a handler via BSD sigaction (syscall 0x200002e, SIGSYS=12) using hand-built structs, then invokes ten syscall classes (0x0-0x7000, class1 mach, class2 BSD, class3-5, huge) via raw svc #0x80 to see if any raise SIGSYS instead of killing the process. It showed only whether sigsys_seen was set and printed each return value, then reported "DONE - no process was killed".
research/probes/sl.c: sl.c is a macOS probe that checks how many named Syslib symbols resolve by parsing dyld shared cache images and their symbol tables. It calls syscall 294 with flag 0x2000000 via svc #0x80 to get the cache, then walks each image's LC_SEGMENT_64 and LC_SYMTAB to match undefined external symbols with a leading underscore. It printed which of the 52 library functions were MISSING versus resolved.
research/probes/slcheck.c: slcheck.c prints sizeof and selected offsetof values of a reconstructed Syslib struct to verify layout matches cosmopolitan's runtime expectations, checking fields like magic, mmap, pthread_self, exit, getentropy, dlopen, and sysctlnametomib. Its mechanism is compile-time offsetof/sizeof reporting, not syscalls or load commands. It records no observed results, only prints offsets for comparison.
research/probes/sr.c: Freestanding arm64 probe that calls BSD syscall 294 (shared_region_check_np, encoded as 0x2000000|294) via raw svc #0x80 to locate the dyld shared cache base, then hex-dumps the header magic and the dyld_cache_header imagesOffset (0x10) and imagesCount (0x14) fields; it exits via syscall 0x2000001 and writes output via 0x2000004, with no libSystem imports.
research/probes/sym.c: A macOS arm64 probe that resolves libSystem symbols with zero imports by walking the dyld shared cache (via syscall 0x2000000|294, shared_region_check_np) to find the libSystem.B.dylib image and parsing its LC_DYLD_EXPORTS_TRIE export trie. It uses raw SVC write syscall 0x2000004 for output, mach_header_64 offsets (ncmds at 16, cmds at 32), and checks the high bit to skip stubs. It prints the resolved addresses of getpid, mmap, and pthread_create.
research/probes/symtab.c: This probe parses the Mach-O LC_SYMTAB command from the dyld shared cache image to test which fileoff-to-memory delta makes the string table and symbol table readable. It walks load commands (__TEXT, __LINKEDIT, LC_SYMTAB at cmd 0x2), tries four candidate deltas, validates string table names, and resolves _getpid via nlist_64 entries, cross-checking against dlsym/dladdr truth.
research/probes/symtab2.c: This probe parses the dyld shared cache's Mach-O load commands to locate __LINKEDIT and LC_SYMTAB, then computes the slide-relative delta to map symtab/strtab file offsets into memory. Its key mechanisms are syscall 294 (shared_region_check_np) via svc 0x80, parsing LC_SEGMENT_64 (0x19) and LC_SYMTAB (0x2), and slide = image_base - __TEXT.vmaddr to translate n_value into a runtime address. It checks whether _getpid in the symtab matches the dlsym ground truth, printing MATCH or differ.
research/probes/sysobj.c: This file implements an ABI-stable syscall layer for a macOS arm64 APE loader using raw BSD-class syscalls (0x2000000 | n from sys/syscall.h, invoked via svc #0x80 with number in x16) instead of libSystem imports. It provides svc6/svc_errno wrappers that translate Darwin's carry-flag (NZCV bit 29) error convention into -errno, and Syslib-compatible w_* wrappers for fork, pipe, mmap, mprotect, munmap, openat, read/write, getentropy, sigaction, sigaltstack, sysctl, rlimits, kill, semaphores, pselect, uid/gid queries, and exit, plus a self-test cmain (called from _main via ldr x0,[sp]) that exercises each wrapper and exits 0. The file documents no actual run results beyond the self-test asserting all wrappers are callable.
research/probes/tiny.s: tiny.s probes ARM64 syscall encoding with svc #0x80, loading syscall number 0x2000001 (exit) into x16 and status 9 into x0. It tests whether the macOS BSD syscall convention (class in high bits via x16) is required versus plain syscall numbers.
research/probes/tk.c: A macOS arm64 probe using raw svc #0x80 mach traps task_self_trap(27) and task_info(44, TASK_DYLD_INFO=17) to fetch the dyld_all_image_infos struct and walk the loaded image list, avoiding shared-cache header parsing. It prints task port, kernel return, all_image_info_addr and size, version at offset 0, infoArrayCount at offset 4, infoArray pointer at offset 8, then each 24-byte image entry (load address, path, modDate), highlighting libSystem.B.dylib, and writes output via BSD syscall 4 (write). The file does not state whether it succeeded or what it found.
research/probes/trap.c: This probe tests direct Mach trap invocation on macOS arm64 via svc #0x80 with class-1 trap numbers (0x1000000|n), checking trap 3 (mach_absolute_time), trap 28 (mach_task_self), and trap 44 (task_info with TASK_DYLD_INFO=17). It uses task_info to read dyld info (version and infoArrayCount at offsets 0 and 4) for APE loader research. Output only flags whether mach_absolute_time looks plausible; no explicit conclusions are recorded.
research/probes/trie.c: Parses a live Mach-O dylib's export trie from memory (LC_DYLD_INFO_ONLY cmd 0x80000033, __LINKEDIT LC_SEGMENT_64 cmd 0x19) to locate "getpid", using dlopen/dladdr into /usr/lib/system/libsystem_c.dylib for ground truth. Walks ULEB128-encoded trie nodes (child count, edge labels, offsets, terminal flags+address) with a 100000-node visited cap. Prints whether the resolved export address matches dlsym, testing trie parsing logic for the APE loader.
research/probes/trie2.c: This probe parses a Mach-O export trie in a loaded libsystem image to resolve symbol names to addresses, locating __LINKEDIT and LC_DYLD_INFO_ONLY (0x80000033) via dladdr, computing the slide as imgbase minus __TEXT vmaddr, and adding the trie offset to the image base. For names like getpid, mmap, and dlsym it cross-checks its trie lookup against dlsym and reports MATCH, present-but-differs, or MISSED.
research/probes/trie3.c: A macOS probe that parses the shared cache exports trie to test whether LC_DYLD_EXPORTS_TRIE dataoff (cmd 0x80000033) is an offset from the cache base rather than the image, using syscall 0x2000000|294 (shared_region_check_np) to get the cache base. It finds "getpid" via the trie, computes the target as cacheBase+dataoff, and compares img+offset against the dlsym/dladdr ground truth, reporting MATCH or DIFFER.
research/probes/tryexec.c: tryexec.c probes execve failure behavior by exec'ing argv[1] with an empty environment and printing errno if it returns. It tests whether macOS arm64 rejects unsigned or malformed Mach-O binaries at execve, a key question for APE loader research.
research/probes/two.c: A minimal C probe returning exit code 5, likely used to test whether a macOS arm64 APE loader faithfully preserves process exit status. It has no syscalls, offsets, load commands, or flags of its own, and the file states no result.
research/probes/usefake.c: A minimal probe with an empty main returning 0, used as a stub or placeholder in macOS arm64 APE loader research. It tests nothing and uses no syscalls, offsets, load commands, or flags. It reports no results.
research/probes/verify.c: This probe checks if APE loader corrections about raw BSD syscalls and mach traps are right. It calls raw svc #0x80 with syscall numbers 42 (pipe, 0x2000000|42) and 101 (nanosleep), and mach trap variants 0x1000000|28 and -28 for task_self, using nzcv carry flag to detect errors. Output decides whether pipe writes both fds, whether nanosleep(101) kills the child with SIGSYS, and whether -28 is the real mach trap versus 0x1000000|28.
research/probes/wonly.s: This probe calls the macOS write syscall (number 0x2000004 via x16, using exit 3) from a minimal ARM64 _main, likely to test whether syscalls work inside an APE-loaded binary. It only invokes write with fd in x0=3 and no valid buffer, so it likely faults or returns an error.
research/probes/x.s: x.s is a minimal macOS arm64 probe that exits via syscall 0x2000001 (exit) with status 42 to verify raw syscall invocation; it confirms syscall-based exit works without libc.
research/probes/z1.s: z1.s is a raw arm64 probe that issues a direct write syscall (x16=0x2000004, svc #0x80) to print "z1 raw svc, no imports", then an exit syscall (0x2000001) with status 7, using no imports or libc.
research/probes/zres.c: A zero-import macOS arm64 probe that scans the shared cache via syscall 294 (shared_region_check_np) and syscall 4 (write) to resolve libc symbols from Mach-O LC_SYMTAB/LC_SEGMENT_64 load commands in dylibs using 0x4000 page offsets, reporting that getpid, mmap, mprotect, pread, getentropy, pthread_create, sysctl, sysctlbyname, exit, and dlsym are found by printing their addresses.
research/probes/zres2.c: A zero-import macOS arm64 probe that locates the dyld shared cache via syscall 294 and resolves libSystem symbols by walking the cache image array (offsets 0x1c0/0x1c4, 32-byte image-info entries) and each image's LC_SYMTAB, using raw svc #0x80 with no libc. It matches underscores per Mach-O convention, requires N_SECT defined symbols, and applies the slide from sharedRegionStart (0xe0) to reach unslid n_value addresses. It resolved getpid by content and reports how many of the loader's ~50-symbol Syslib set were found.
