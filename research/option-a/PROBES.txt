Files removed from research/option-a. Full sources: git show 1785223:<path>

research/option-a/apexdemo.c: apexdemo.c is a maximal Option-A Mach-O arm64 test with zero dylib load commands and minos 11.0 that uses raw svc #0x80 BSD syscalls (write 4, exit 1, shared_region_check_np 294, getpid 20) for I/O and discovery; it walks shared-cache image info at offsets 0xE0/0x1C0/0x1C4, parses LC_SEGMENT_64 0x19, LC_DYLD_EXPORTS_TRIE 0x80000033, and LC_DYLD_INFO_ONLY 0x80000022, then resolves exports by content via export-trie flags 0x08/0x10 and calls function pointers. It prints cache base and found/not-found results for getpid/write/mmap/pthread_create/getentropy/dlopen/sysctl, and if getpid resolves it compares the content-discovered host getpid() with raw getpid(), but the file lists no actual run output.
research/option-a/auxv.c: research/option-a/auxv.c is a macOS arm64 stack/auxv probe that prints argc, sp, and auxv type/value pairs by reading sp, taking argc at p[0], skipping argv and envp, then walking auxv pairs until AT_NULL=0. It uses raw syscalls write=0x2000000|4 and exit=0x2000000|1 via svc #0x80, loops up to 64 q+=2 pairs, and marks values in [0x180000000,0x1A0000000) as cache-window pointers, with no load commands or flags used. The file states no observed result/output.
research/option-a/basefallback.c: basefallback.c tests finding the macOS arm64 shared cache base without syscall 294 by scanning upward from SP within 4 MiB in 8-byte steps for a 16 KiB-aligned pointer in 0x180000000-0x1A0000000 with a valid "dyld_v1" header (checking imagesOffset at +0x1C0 in 0x200..0x10000 and imagesCount at +0x1C4 in 1..100000) or for the literal magic, using raw write/exit syscalls 0x2000000|4 and 0x2000000|1; it then cross-checks the found base against syscall 294 (0x2000000|294). It prints "FALLBACK SUCCEEDED" or "fallback found nothing" and equality with the 294 result, but the file records no actual observed outcome.
research/option-a/cacheprobe.c: cacheprobe.c opens and mmaps a dyld_shared_cache_arm64e file (default /System/Volumes/Preboot/Cryptexes/OS/System/Library/dyld/dyld_shared_cache_arm64e) and parses/dumps dyld shared cache header fields at offsets 0x10-0xF0 and 0x1C0-0x1C8, including mappingOffset/Count, imagesOffset/Count, dyldBaseAddress, code signature, cache type/subtype, shared region, and maxSlide. It validates the images table, prints up to 5 ImgInfo entries using 32-byte stride and pathFileOffset strings plus up to 40 MapInfo mappings, and uses open/fstat/mmap with PROT_READ|MAP_PRIVATE but no syscall numbers or load commands; it states no observed result.
research/option-a/cacheprobe2.c: cacheprobe2.c mmaps the arm64e dyld shared cache and finds an image by path substring (default libSystem.B.dylib), using cache-header offsets CH_IMAGES_OFF 0x1C0, CH_IMAGES_CNT 0x1C4, CH_IMGS_TEXT_OFF/CNT 0x88/0x90, and mapping-table offset/count 0x10/0x14 to convert the unslid __TEXT load address to a file offset and dump the Mach-O header and load commands. It decodes LC_SEGMENT_64, LC_SYMTAB, LC_DYLD_INFO(_ONLY), LC_DYLD_EXPORTS_TRIE, and related commands while printing magics, flags, segments, and dyld-info fields, using open/fstat/mmap rather than explicit syscall numbers. The file itself reports no observed results.
research/option-a/cacheresolve.c: cacheresolve.c resolves symbols on macOS arm64 by reading the dyld shared cache via syscall 294, computing slide = cacheBase - *(cache+0xE0), walking the image table at cache+*(0x1C0) with count *(0x1C4), and parsing each image's LC_DYLD_EXPORTS_TRIE/LC_DYLD_INFO_ONLY export trie without dlopen/dlsym. It walks ULEB128 tries handling ADDRESS, ABS (0x02), THREAD_LOCAL (0x04), REEXPORT (0x08), and STUB_AND_RESOLVER (0x10), follows reexport ordinals, and validates candidate __LINKEDIT trie placements by counting symbols. It encodes findings that imagesOffset is 0x1C0, imagesCount 0x1C4, old fields 0x18/0x1C are unused, names carry a leading underscore, and that STUB_AND_RESOLVER's first ULEB is the callable implementation while dlsym may return a different CPU-optimized address.
research/option-a/calltest.c: calltest.c tests whether content-discovered libc/pthread function pointers from cacheInit/resolveByContentEx are actually callable by resolving symbols (getpid, strcmp, strncmp, memcpy, strlen, pthread_self, getentropy, malloc, free, sysctl) and comparing calls with direct libc calls; no syscall numbers, offsets, load commands, or flags appear in this file. It prints OK/BAD for strcmp, strncmp, malloc/free, getpid, and pthread_self, with pthread_self allowed to differ.
research/option-a/chdr.c: Prints offsetof/sizeof values for dyld_cache_header fields (imagesOffset, imagesCount, subCacheArrayOffset, subCacheArrayCount, mappingWithSlideOffset, mappingWithSlideCount, imagesTextOffset, patchInfoAddr) and dyld_subcache_entry from dyld_cache_format.h, with TARGET_OS_SIMULATOR and TARGET_OS_EXCLAVEKIT set to 0; no syscalls, load commands, or flags. It is a layout probe for the macOS arm64 shared-cache header and does not report observed values.
research/option-a/cov.c: cov.c uses syscall 294 to get the shared cache, computes slide from offset 0xE0, reads imagesOffset/Count at 0x1C0/0x1C4, and counts export-trie symbols per Mach-O via __LINKEDIT (LC_SEGMENT_64 0x19) plus LC_DYLD_EXPORTS_TRIE 0x80000033 or LC_DYLD_INFO_ONLY 0x80000022, validating MH_MAGIC_64 0xfeedfacf and walking ULEB tries. It prints images, parsed-ok, no-trie, bad, and total-symbols; no result is shown in the file.
research/option-a/dis.c: dis.c is a small diagnostic tool that takes one or more hex addresses and dumps 12 consecutive 32-bit words from each address at offsets +00 through +2c in hex, likely for inspecting memory or instructions in the APE loader research; it uses strtoull/printf and includes sys/syscall.h but makes no syscalls and references no syscall numbers, load commands, or flags; it reports no findings.
research/option-a/dis2.c: dis2.c dumps 12 32-bit words from each hex address argument at offsets +0x00..+0x2c for manual inspection of memory/structures in the APE loader research; it uses no syscall numbers, load commands, or flags (sys/syscall.h is included but unused), and the file reports no results.
research/option-a/dup.c: Uses syscall 294 (shared_region_check_np) to get the macOS arm64 shared cache base, derives the ASLR slide from the stored pointer at offset 0xE0, then walks the image table at offsets 0x1C0/0x1C4 reading ImgInfo entries (address, modTime, inode, pathFileOffset) and prints each matching image's mach_header_64 magic, cpusubtype, filetype, ncmds and flags for paths containing argv[1]. It only reports a total match count; no findings are recorded.
research/option-a/flagsdbg.c: flagsdbg.c is a diagnostic that underscore-prefixes an input symbol, gets the dyld shared-cache base via syscall 294, computes the slide using offset 0xE0, iterates cache images from offsets 0x1C0/0x1C4, validates each Mach-O header at 0xfeedfacf, and parses load commands 0x19 (__LINKEDIT), 0x80000033 (exports trie), and 0x80000022 (dyld info) before walking the export trie. For each matching image it prints the image path, raw terminal bytes, termSize, and decoded export flags REEXPORT=8, STUBRESOLVER=0x10, ABS=2, TLS=4, but the file states no overall findings or conclusions.
research/option-a/grace.c: Tests resolveByContentEx's graceful not-found path after cacheInit by resolving three bogus symbol names and expecting 0 with "cleanly not found" instead of an unexpected address; this file contains no syscall numbers, offsets, load commands, or flags, and it does not report actual runtime results.
research/option-a/lc2.c: lc2.c probes a single loaded macOS arm64 image by name using syscall 294 (shared_region_check_np) to get the shared cache base, then derives the slide from the cache header's start field at offset 0xE0 and locates the image list via the images offset/count fields at 0x1C0/0x1C4. For the matching mach_header_64 (magic 0xfeedfacf) it parses load commands (ncmds at +16, sizeofcmds at +20, first LC at +32) and prints names for dylib-type commands. It tests recognition of LC_ID_DYLIB(0xc)/LC_LOAD_DYLIB(0xc), LC_LOAD_WEAK_DYLIB(0x18), LC_REEXPORT_DYLIB(0x1f), LC_LOAD_UPWARD_DYLIB(0x23), and LC_LAZY_LOAD_DYLIB(0xe) in the shared cache.
research/option-a/lcdump.c: This research utility on macOS arm64 uses syscall 294 (a shared-cache query) to obtain the dyld shared cache base pointer, then computes the ASLR slide from the header's slide info offset 0xE0 and locates a named image via the images offset/count at 0x1C0/0x1C4, walking its Mach-O header (magic 0xfeedfacf) to print load command numbers, sizes, and names (LC_SEGMENT, LC_SEGMENT_64, LC_UUID, LC_DYLD_INFO_ONLY, LC_DYLD_CHAINED_FIXUPS, etc.). It verifies that a target dylib can be found and its load commands enumerated through direct memory reads, useful for APE loader research into manipulating shared-cache images.
research/option-a/liveprobe.c: liveprobe.c introspects the in-process macOS shared cache: it calls BSD syscall 294 shared_region_check_np to get cacheBase, reads cache header offsets 0xE0/0xE8/0xF0 for srStart/srSize/maxSlide to compute slide and 0x1C0/0x1C4 for images, then finds libSystem by image path and dereferences its live mach_header_64 after checking magic 0xfeedfacf. It walks load commands for LC_SEGMENT_64 __TEXT/__LINKEDIT, LC_DYLD_INFO(_ONLY), LC_DYLD_EXPORTS_TRIE, LC_SYMTAB, and LC_DYSYMTAB, printing __LINKEDIT vmaddr and export trie/symtab offsets. The file only prints observations and does not state a fixed result.
research/option-a/locate.c: research/option-a/locate.c uses syscall 294 to get the live macOS arm64 shared-cache base, reads cache header offsets 0xE0/0x1C0/0x1C4/0x188/0x18C to compute slide and dump image/subcache arrays, parses matching Mach-O images (magic 0xfeedfacf, LC_SEGMENT_64, LC_DYLD_EXPORTS_TRIE, LC_DYLD_INFO_ONLY) for __LINKEDIT and export-trie dataoff/datasize, then tests translations L+(expOff-linkFileOff), L+expOff, cacheBase+expOff, slide+expOff, imgBase+expOff, and each subcache cacheVMOffset+expOff with a ULEB export-trie lookup for a symbol (default pthread_create) plus dlopen/RTLD_LAZY dlsym; the file prints candidate first bytes and hit results but states no conclusion.
research/option-a/minimal.c: A minimal C program whose main just returns argc, serving as a trivial baseline payload for the arm64 APE loader's argv/handoff behavior; it contains no syscall numbers, offsets, load commands, or flags and states no results.
research/option-a/munge.c: munge.c is a Mach-O load-command patcher for macOS arm64 APE-loader research: it mmaps a thin arm64 Mach-O (magic 0xfeedfacf), then can show commands, rename dylib/dylinker names, set filetype at header offset 0x0C, set hdrflags, or drop/compact LC_LOAD_DYLIB, custom LC_LOAD_WEAK_DYLIB (0x18|LC_REQ_DYLD), and LC_ID_DYLIB while recomputing ncmds/sizeofcmds. It uses no explicit syscall numbers (plain open/mmap), reads dylib name offsets from load-command fields, and warns that the code signature must be refreshed with codesign -f -s - FILE; it states no experimental result beyond its command output.
research/option-a/one.c: research/option-a/one.c is a macOS arm64 APE-loader probe that calls syscall 294 to get the shared-cache base, reads dyld_cache_header offsets 0xE0/0x1C0/0x1C4, selects image 15, and parses its Mach-O LC_SEGMENT_64 (0x19) __LINKEDIT plus LC_DYLD_EXPORTS_TRIE (0x80000033) or LC_DYLD_INFO_ONLY (0x80000022) export info. It computes two candidate export-trie addresses from __LINKEDIT vmaddr/fileoff and export offset, then walks both with ULEB128 to find "_"+argv[1], printing FOUND flags/bytes or not found for each pick; it reports no conclusion beyond those prints.
research/option-a/renamesim.c: renamesim.c tests whether an export-only dyld shared cache resolver still resolves symbols when image paths containing libSystem.B, libSystem, or libdyld are ignored, using cacheInit/g_cache, image info at CH_IMAGES_OFF 0x1C0, Mach-O 0xfeedfacf parsing, LC_SEGMENT_64 0x19 __LINKEDIT, LC_DYLD_EXPORTS_TRIE 0x80000033 or LC_DYLD_INFO_ONLY 0x80000022, and export-trie ULEB flags 0x10/0x08/0x02, with no direct syscalls. It reports resolving 8/8 symbols with those hides and 0/8 when hiding any path containing lib, proving path names are not needed.
research/option-a/restest.c: restest.c tests whether naive dyld shared-cache export-trie walking is enough for STUB_AND_RESOLVER symbols by locating stub/resolver pairs and calling resolvers for _platform_strcmp/_platform_strncmp/_platform_memmove/_platform_bzero; it uses cache image offsets 0x1C0/0x1C4, Mach-O magic 0xfeedfacf, load commands LC_SEGMENT_64 (0x19), LC_DYLD_EXPORTS_TRIE (0x80000033), LC_DYLD_INFO_ONLY (0x80000022), __LINKEDIT vmaddr/fileoff conversion, and trie flag 0x10, with no direct syscall numbers. It prints each resolver's return and stub/impl bytes, but the file reports no observed result beyond the stated goal.
research/option-a/rq.c: This macOS arm64 tool syscall(294) (shared_region_check_np) to get the dyld shared cache base, computes the slide from the cache header magic at offset 0xE0, then walks the cache's image list at header offset 0x1C0/0x1C4 to find images by path. For a matching image it parses Mach-O load commands to locate __LINKEDIT plus exports-trie info from LC_DYLD_EXPORTS_TRIE (0x80000033) or LC_DYLD_INFO_ONLY (0x80000022), walks the ULEB exports trie to find the symbol, prints its flags, offsets and computed live addresses, and compares dlsym output.
research/option-a/scaletest.c: scaletest.c builds two large symbol lists (Syslib contract plus broad libc set), calls cacheInit/cacheInfo and resolveByContentEx from cacheresolve.c to resolve each symbol by content, compares the result with dlsym(RTLD_DEFAULT), and counts/prints cache base, slide, image count, MATCH, DIFF, NOTFOUND, and dlsym==0 cases; it uses no explicit syscall numbers, offsets, load commands, or flags itself, relying only on the resolver and dlsym; the file contains no result output, so it shows no coverage numbers.
research/option-a/stub.c: research/option-a/stub.c is a placeholder C stub whose only function _stub_placeholder returns 0; it contains no syscall numbers, offsets, load commands, flags, or reported findings.
research/option-a/stubfinal.c: stubfinal.c locates libsystem_platform.dylib in the macOS arm64 dyld shared cache via syscall 294 and cache offsets 0xE0/0x1C0/0x1C4, then parses Mach-O load commands 0x19/0x80000033/0x80000022 to walk the exports trie for STUB_AND_RESOLVER leaves (flag 0x10) and compute resolver addresses r1/r2 for __platform_strcmp, __platform_strncmp, __platform_memmove, and __platform_bzero. It compares those with dlsym(RTLD_DEFAULT, ...) and calls r1 to verify strcmp/strncmp/memmove/bzero semantics, but the file records no actual results.
research/option-a/stubprobe.c: stubprobe.c probes the dyld shared cache for a STUB_AND_RESOLVER export (__platform_strcmp) to determine which ULEB value is the arm64e resolver and which is the callable implementation. It gets the cache via syscall 294, uses header offsets 0xE0/0x1C0/0x1C4 and image 15 (libsystem_platform), parses LC_SEGMENT_64 0x19 for __LINKEDIT plus LC_DYLD_EXPORTS_TRIE 0x80000033 or LC_DYLD_INFO_ONLY 0x80000022, decodes the export trie terminal's flags/u1/u2, calls u1 as resolver(u2_live), and compares the result to dlsym(strcmp) with an expected strcmp('abc','abd') == -1. The file prints flags/u1/u2, the resolved impl, dlsym(strcmp), and the strcmp result but does not embed a final observed outcome.
research/option-a/stubprobe2.c: stubprobe2.c probes STUB_AND_RESOLVER exports in libsystem_platform by using syscall 294 to get the shared cache, computing the slide from shared-cache header offset 0xE0, finding images via offsets 0x1C0/0x1C4, parsing Mach-O load commands 0x19 __LINKEDIT, 0x80000033 LC_DYLD_EXPORTS_TRIE, and 0x80000022 LC_DYLD_INFO to read the export trie, then extracting flag 0x10 entries and their first two ULEBs (r1, r2) for names such as _platform_strcmp and _platform_memmove; it calls r1/r2 as real functions and compares them with dlsym for strcmp and memmove cases. The file reports no conclusion beyond printing those probe results.
research/option-a/subs.c: subs.c dumps each dyld shared-cache subcache header and mapping table, using syscall 294 to get the cache base, checking dyld_v1 magic, and reading mappingOff/mappingCount at 0x10/0x14, subcacheOff/Count at 0x188/0x18C, and srStart at 0xE0. For each cache it prints up to 8 MapInfo records with address, size, fileOffset, and maxProt/initProt flags; it parses no load commands. It reports no cache or NO MAGIC on failures but states no actual findings.
research/option-a/syslibdemo.c: syslibdemo.c builds a full arm64 APE Syslib table by content discovery, resolving all 55 required symbols from the dyld shared cache with no dylib load commands and no undefined symbols. It locates the cache via syscall 294, walks each image's trie export table parsing LC_DYLD_INFO(_ONLY)/LC_DYLD_EXPORTS_TRIE (0x80000022/0x80000033) and LC_ID_DYLIB (0x19), and handles address, reexport, stub, abs, and tls entries. It reports resolved count and proves callability by invoking fork, write, and getentropy through the discovered pointers.
research/option-a/tlib.c: This tiny C stub defines two exported symbols, my_export_one returning 1 and my_export_two returning x+2, for APE loader research. It tests nothing itself and contains no syscall numbers, offsets, load commands, or flags, and reports no results.
research/option-a/trie3.c: trie3.c is an in-process dyld shared-cache symbol resolver that uses syscall(294, &cacheBase) (shared_region_check_np), walks images via offsets CH_SR_START=0xE0, CH_IMAGES_OFF=0x1C0, CH_IMAGES_CNT=0x1C4, parses LC_SEGMENT_64/__LINKEDIT plus LC_DYLD_EXPORTS_TRIE or LC_DYLD_INFO(_ONLY) export tries with ULEB128, handles EXPORT_SYMBOL_FLAGS_REEXPORT=0x08, and compares results to dlsym. It tests three candidate trie pointer translations: parent __LINKEDIT live + expOff - linkeditFileOff, __LINKEDIT live + expOff, and cacheBase + expOff. The file prints per-image results and the dlsym comparison but states no conclusion.
research/option-a/triedump.c: triedump.c parses a Mach-O file's dyld export trie from disk and dumps exported/reexported symbols, walking ULEB128 trie nodes found via LC_DYLD_EXPORTS_TRIE or LC_DYLD_INFO(_ONLY) using dataoff/datasize or export_off/export_size and mmap/open/fstat to read the file. It prints EXPORT entries with address and flags and treats terminal flags with 0x08 as REEXPORT, with no syscall numbers used. It reports no observed result beyond the symbol dump.
research/option-a/tryslice.sh: tryslice.sh takes a Mach-O file, extracts its arm64e or arm64 slice with lipo into /tmp/apex-a/slice.bin, falling back to copying the file, then runs /tmp/apex-a/triedump on the slice to dump its export trie; it lists no syscall numbers, offsets, load commands, flags, or results.
research/option-a/valid.c: valid.c tests a bounds-checking validator for a dyld shared cache header by copying the real header to a buffer and corrupting fields. It checks the "dyld_v1" magic, then validates the table offset at 0x1C0 (must be 0x200-0x10000) and image count at 0x1C4 (1-100000, table must fit in available bytes). It reads the kernel cache pointer via syscall 294 (SYS_shared_region_check_np) and showed that corrupted offset, count, or magic values all make validate return 0 without crashing.
research/option-a/vec.c: Dumps the LC_MAIN fourth-argument (x3) arm64 macOS apple vector, printing 12 pointer-sized entries and probing entries 0..11 as char* strings to try to find the shared cache base without syscall 294. Uses raw svc #0x80 syscalls with x16 carrying 0x2000000|4 for write and 0x2000000|1 for exit. The comments say the vector is 12 pointers then string data, but the file reports no observed results.
research/option-a/vec2.c: vec2.c is an arm64 macOS experiment that dumps saved_x3 from an asm shim and tests whether the Apple vector can yield the shared cache base without syscall 294; it uses svc #0x80 syscalls 0x2000000|4 (write) and 0x2000000|1 (exit), prints 16 u64 vector slots, tries string interpretation, and scans 0x4000 bytes at 8-byte steps for 'dyld_v1' magic; the file records no observed result.
research/option-a/x3probe.S: x3probe.S saves x0/x3 at LC_MAIN entry, manually formats x3 and x0 as 16 hex digits into a 40-byte stack buffer at offsets 0-39, and writes it using raw macOS arm64 write(2) with x16=0x2000004, svc #0x80, then exit(0) via x16=0x2000001. It probes what value x3 carries at entry before any C code touches it, but the file records no observed result.
research/option-a/x3shim.S: x3shim.S is an arm64 assembly shim that at LC_MAIN entry saves the raw x3 (Apple vector) and x0 into globals _saved_x3 and _saved_x0, then branches to _c_main. It uses adrp/add page-relative addressing and str, with no syscall numbers, offsets, load commands, or flags shown. It tests/preserves the Apple x3 vector for C-side inspection, but does not state a result.
