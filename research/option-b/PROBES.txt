Files removed from research/option-b. Full sources: git show 1785223:<path>

research/option-b/BUILD.md: This build-notes file gives clang/ld command lines for a freestanding macOS arm64 APE-style loader, building `zfinal` (prefix pruning plus cached image table) and `mydlsym` with `rs_syscall.s`, `-ffreestanding -fno-stack-protector`, `-weak-lSystem`, `-e _main`, and an `nm -u` empty-symbol check. It also tests weak vs strong dylib binding (cases A/C/D abort, E runs) and documents SIGKILL on unsigned in-place install-name edits. No explicit result beyond the stated expectations is reported.
research/option-b/SOURCES/classify.c: This ARM64 macOS research tool dlsym()s a list of libSystem functions and classifies each as real-C, direct-svc, svc-later, or mach-svc by scanning the first 12 instructions for svc #0x80 (0xd4001001) and movn/movz into x16 (masks 0xff800000 against 0x92800000/0xd2800000, rd=16). It prints each symbol's kind, first four instruction words, and svc/movn instruction offsets, but the file itself reports no results.
research/option-b/SOURCES/dsym.txt: A macOS arm64 symbol address dump mapping libSystem/dyld functions like mmap, pthread_jit_write_protect_np, and getentropy to their runtime addresses. It provides no test description or result; pthread_stack_min is listed as 0x0.
research/option-b/SOURCES/forkchk.c: Fork-behavior probe for the macOS arm64 APE loader that calls raw syscalls: getpid (20), fork (2), and getppid (39), then prints p0, p1, ppid, fork's raw x0 return, and whether the pid changed. It tests whether fork works under the loader and reports the parent/child split via is_child. The file states no observed result, only the raw values it prints.
research/option-b/SOURCES/hedge/hres.c: hres.c is a zero-import APE test that scans the dyld shared cache export tries for _malloc and _getpid after libSystem is renamed so it never loads, using syscall 78 (vm_map-style probe), Mach-O 0xfeedfacf/0x19/0x80000033 parsing, and dyld cache offsets 0x88/0x90/0x180000000. It resolves both symbols via the trie walker and calls them: getpid works as pure code, but calling malloc crashes because resolvable symbols do not imply usable libc state.
research/option-b/SOURCES/hedge/libhedge.c: A tiny placeholder C file defining hedge_anchor returning 7, used as an anchor symbol for the libhedge component in the option-b APE loader research; it has no syscalls, offsets, load commands, or flags, and reports no test results.
research/option-b/SOURCES/hedge/main.c: Minimal C program whose main ignores argc, argv, and envp and returns exit code 42; it uses no syscall numbers, offsets, load commands, or flags and reports no findings.
research/option-b/SOURCES/hedge/rs_syscall.s: A macOS arm64 syscall wrapper for rs_syscall6 that moves a class-tagged syscall number into x16 and args into x0-x5 before svc #0x80. It supports BSD (0x2000000|n) and mach (0x1000000|n) classes and converts carry-set errors to -errno. The file states no observed results.
research/option-b/SOURCES/imgtest.c: This macOS arm64 tool probes a live shared cache at fixed base 0x188830000, reading the imagesTextOffset/imagesTextCount at offsets +0x88/+0x90 and the first six 32-byte image entries (uuid, loadAddress u64, textSegmentSize u32, pathOffset u32). It validates each path pointer with SYS_mincore on a 16384-byte page and prints the path if mapped, using mincore bit 0x80 to detect unmapped pages. It prints the parsed offsets and image paths, but the source records no results.
research/option-b/SOURCES/mt2.c: mt2.c probes Mach trap syscall encoding on arm64 by calling rs_syscall6 with negative x16 values derived from MOVN immediates mapped to task_self, host_self, thread_self, and mach_reply_port traps. It tests -28, -29, -27, and -26 traps, printing returned port names and re-calling -28 to check stability. It concluded negative x16 IS the mach trap encoding since all calls returned normally.
research/option-b/SOURCES/mt3.c: Tests macOS arm64 Mach trap return handling by issuing svc #0x80 with x16=-28 (task_self_trap), reading kern_return_t in x0 without consulting carry, and printing four results; it concludes Mach traps return status in x0 and must not use the carry/negate convention applied to BSD syscalls.
research/option-b/SOURCES/mydlsym.c: This file implements and tests my_dlsym, a zero-import dlsym replacement for macOS arm64 that locates the dyld shared cache at fixed address 0x180000000 by scanning for the "dyld" magic (0x646c7964) and parses Mach-O headers (magic 0xfeedfacf) and LC_SEGMENT_64 (0x19) load commands plus chained-fixups/export data commands (0x80000033, 0x80000022) to find the exports trie. It walks the trie via ULEB128 decoding and a recursive tree walk matching underscore-prefixed symbol names such as getpid, malloc, and pthread_mutex_lock, then invokes resolved functions like getpid and pthread_self to prove the pointer works.
research/option-b/SOURCES/p_a.c: Option-B test probe p_a.c calls o_lbl logging and issues BSD syscall 42 (pipe) via rs1 on a macOS arm64 APE loader, printing the return value and the two pipe fd slots before and after the call, showing whether the loader lets main reach and complete the pipe syscall.
research/option-b/SOURCES/p_class.c: p_class.c calls rs_syscall6 with a decimal classnum from argv[1] and no other args, then prints the classnum and raw return code. It probes how macOS arm64 handles unspecified syscall class numbers via the rs shim. It does not state a result.
research/option-b/SOURCES/p_fork4.c: This APE option-b test forks a child using raw syscall 2 (fork) with rs0 and checks getpid (20) to distinguish parent from child, then the parent calls wait4 (7) on the child pid and prints the raw status, wifexited and exit code. It also uses syscall 42 (pipe) and syscall 1 for output, exercising fork/wait/pipe semantics under the loader. The child exits via rs1(1,0) and prints whether forkrc==0 or getpid changed; the file records no explicit result text beyond these printed diagnostics.
research/option-b/SOURCES/p_min.c: p_min.c probes macOS arm64 mincore availability by issuing syscall 78 via rs3 for a list of addresses with a 16K page size and printing rc and vec[0]. It also walks down from 0x188963000 in 0x4000 steps, stopping at the first unmapped page or checking for Mach-O magic 0xfeedfacf. The file records no conclusion, only the probe output format.
research/option-b/SOURCES/p_sig4.c: p_sig4.c tests whether an APE loader can install a SIGBUS handler (sigaction syscall 46 with a raw sa_handler) and survive a wild load; the naked handler bumps g_hits via adrp/add, exits with syscall 1 (exit) using x0=0x5a. It installs with mask=0/flags=0, faults by reading address 0x100000000, and would print hits if the handler ran, but no output result is recorded.
research/option-b/SOURCES/p_sig6.c: This test installs a SIGBUS (signal 10) SA_SIGINFO handler (flags 0x40) via syscall 46, then deliberately faults by reading address 0x100000000 to see if a signal handler works. The naked handler increments a volatile counter g_hits then issues syscall 1 (exit) with x0=0x5b instead of returning, so if execution reaches the "returned" print it means the handler never ran. It tests whether the APE loader delivers SIGBUS to a registered handler, with non-returning handler exiting via svc 0x80 to avoid sigreturn issues.
research/option-b/SOURCES/p_stubs2.c: p_stubs2.c is a macOS arm64 probe that dlsym()s Mach trap and kernel RPC symbols like task_self_trap, mach_msg_trap, and _kernelrpc_mach_vm_allocate_trap, dumping the first six 32-bit words of each stub to reveal their syscall numbers and encoding (either NULL if absent or the mach trap instruction words). It tests which Mach traps are exposed as callable stubs on arm64 for an APE loader, but the file records no observed results.
research/option-b/SOURCES/p_zero.c: p_zero.c tests ARM64 SIGSEGV delivery for a load from address 0 in an APE loader, installing a handler via rt_sigaction (syscall 46, sig 11, struct sigaction, flags 0x40 SA_SIGINFO) and naked handler h. The handler stores 12345 to g_hits then calls write(1, x0=0x5c, 1) using svc #0x80, and main reads *0, prints the return value, and exits via rs1(1,0). The file does not state what the test showed.
research/option-b/SOURCES/probesafe.c: probesafe.c forks a child per address to test whether specific 64-bit addresses around the shared cache region are readable, reporting the byte value, mincore return, and resident vector byte for each; it uses SYS_mincore on a page-aligned 16KB window against a hardcoded address list (e.g. 0x100000000, 0x180000000, 0x188830000, 0x188cdd000, 0x198b70000, 0x1f4bec1a0, 0x1ff05c000). The file records fork-based read success/fault plus mincore residency but states no conclusions.
research/option-b/SOURCES/raw_all.c: This file tests which macOS Syslib functions can be replaced by raw syscalls in an APE loader, running each numbered case (0-15) independently since a SIGSYS kills the process. It probes syscalls like select(93), getentropy(500), pipe(42), fork(2), issetugid(327), thread_selfid(372), sigaltstack(53), sysctl(202), getrlimit(194), nanosleep(101), pread(153), sigaction(46), and mach task_self(mt0 28) via rs0-rs6/mt0 helpers. It notes nanosleep(101) triggers SIGSYS, implying that syscall is unavailable on arm64.
research/option-b/SOURCES/resolved.txt: This file records the final zero-import resolver results for a macOS arm64 APE loader, mapping libc symbols such as _getpid, _mmap, _munmap, _mprotect, _mach_vm_region, and pthread and semaphore functions to absolute addresses under cachebase=0x188830000. It shows several symbols unresolved as 0x0 (_fork, _sigaction, _raise), indicating the resolver did not locate those. It does not state syscall numbers, offsets, load commands, or flags, and does not report an explicit test outcome.
research/option-b/SOURCES/rs.h: This header declares the raw syscall shim for the macOS arm64 APE loader: rs_syscall6 issues BSD syscalls with class 0x2000000 and Mach traps with 0x1000000, plus rs0-rs6/mt0/mt6 wrappers. It also provides minimal output helpers o_write/o_puts/o_putn/o_puth using write syscall number 4 on fd 1. It does not report any test results or offsets/load commands.
research/option-b/SOURCES/rs_syscall.s: This ARM64 assembly file defines _rs_syscall6, a six-argument syscall wrapper that takes a class-tagged number (0x2000000|n BSD, 0x1000000|n mach) in x0, moves args into x0-x5, executes svc #0x80, and negates x0 on carry to convert errors to -errno. Its key mechanism is the class-tag offset encoding plus carry-flag error handling, matching the Linux-style convention rather than raw Darwin. The file states no test results.
research/option-b/SOURCES/sub2.c: Scans mapped 16 KiB pages from 0x180000000 to 0x2c0000000 on macOS arm64 using mincore syscall SYS_mincore to find dyld_v1 shared cache headers by magic string. For each "dyld_v1" header it reads mappingOffset at b+16 and mappingCount at b+20, then prints up to 12 32-byte mapping entries with addr, size, and fileOff at offsets 0, 8, 16.
research/option-b/SOURCES/tbase.c: tbase.c probes whether dyld's shared-cache trie and data region can be parsed on macOS arm64 while running under a foreign loader, using SYS_mincore to validate mapped pages and walking ULEB128-encoded trie nodes to look up export offsets. It tries three base addresses (cachebase+0xb3cce0, 0x18890f000+0xb3cce0, 0x190340000+0xb3cce0) and checks each 16-byte read and whether the "dlsym" lookup equals 0x188910c04.
research/option-b/SOURCES/verify.c: verify.c dlopen/dlsym-probes a list of libc/pthread/dispatch/Mach symbols (getpid, mmap, mach_vm_region, fork, pthread_*, sysctl, sem_*, dispatch_semaphore_*, etc.) via RTLD_DEFAULT and prints each name and resolved address to confirm which are available to a macOS arm64 APE loader.
research/option-b/SOURCES/zfinal.c: A zero-import dyld shared cache symbol resolver test that finds a cache image by LC_ID_DYLIB name via the cache image table (base 0x180000000, offsets 0x88/0x90), uses raw mincore syscall 78 to test mapping, then walks the LC_DYLD_EXPORTS_TRIE export trie to resolve symbols image-relative without a slide. It resolves ~50 libsystem_kernel/pthread/platform/c/dyld/dispatch symbols and prints addresses, but only exercises the primary single-image route since the re-export fallback is disabled by an early return.
research/option-b/SOURCES/zr2.c: zr2.c tests trie-based dlsym symbol resolution against hardcoded macOS arm64 addresses by walking the libdyld export trie with ULEB128 decoding, mincore-based page mapping checks, and a 4M-node budget guard. It computes the trie base as ldvm+slide+trie_off-ldfo, handles terminal payloads including reexport ordinals, and returns imghdr+offset for matches. It reports MATCH/MISMATCH for symbols like _dlsym, _getpid, and _pthread_self against expected addresses.
research/option-b/allids.c: Scans the shared cache 0x180000000-0x220000000 in 0x4000 steps, probing each page readable via syscall 78 (mincore) and the arm64 high-bit check, then validates MH_MAGIC_64 0xfeedfacf and walks ncmds load commands looking for LC_SEGMENT_64 (0x19) with __TEXT and LC_ID_DYLIB (0xd) install names. It prints vmaddr and id for dylibs whose names contain kernel, pthread, libSystem.B, dyld, or libdispatch.
research/option-b/apeinfo.c: apeinfo.c is a static ELF inspector that scans an APE/alpha binary at 64 KiB intervals to locate embedded ELF images and dump their program headers. For e_machine=183 (AArch64) it reads e_entry, e_phoff, e_phnum, e_phentsize and each phdr's type, flags, offset, vaddr, filesz, memsz and align. It only reports layout; the file states no results.
research/option-b/appleentry.s: appleentry.s is a macOS arm64 APE loader stub that captures the kernel-supplied Apple vector pointer from x3 into the global __apple_vec before branching to _main. Its key mechanism is an adrp/add pair to compute __apple_vec's page address, a str of x3 to store it, and a b to _main, with the vector initialized to zero in .data. The file does not state any observed result.
research/option-b/bf2.c: This file brute-forces a dyld shared cache trie to locate the "dlsym" export by scanning candidate trie bases relative to a hardcoded library header address (0x18890f000), walking the trie structure via uleb-decoded terminal sizes, child counts, edge lengths, and offsets. It uses mincore(2) to check page residency before every dereference and filters candidates by a small root terminal-size byte. It confirmed the trie base such that the resolved dlsym offset lands at the expected target address 0x188910c04.
research/option-b/bootroute.c: bootroute.c is a zero-import macOS arm64 research probe that scans the shared-cache address window 0x180000000-0x1a0000000 in 16K steps for Mach-O headers. Its key mechanism is an is_macho() validator checking magic 0xfeedfacf/0xcffaedfe/0xfeedface, cputype ARM (12), command count 1-4096, LC_SEGMENT_64 (0x19) __TEXT size, and a noop slide; the file does not report any hits results itself.
research/option-b/brute.c: Brute-forces the syscall trie base address in a macOS arm64 shared cache to locate the "getpid" handler offset by scanning mapped regions between 0x188000000 and 0x192000000 in 0x100 steps. Uses mincore to test mapping, then walks the kernel trie structure via ULEB-decoded terminal nodes, child counts, edge labels, and child offsets, skipping nodes with the 0x08 flag. Success is when the found offset added to kernel header 0x188cdd000 equals the expected 0x188cde178 (offset 0xf34), printing up to 8 matching bases.
research/option-b/cache.c: This test scans addresses 0x170000000 to 0x1a0000000 in 0x4000 steps, using syscall 78 (mincore) to find mapped readable pages and checks for the little-endian 'dyld' magic 0x646c7964 to locate the dyld shared cache, then dumps 32 bytes hex and ASCII at the match; the file does not state any result.
research/option-b/cache2.c: This macOS arm64 research file dumps the dyld shared cache header qwords, mapping table, and imagesText offset at hardcoded addresses, then probes six candidate addresses for the libSystem trie (trie_off 0x10e6488) by checking mapping validity with syscall 78 and printing the first bytes found. Its key mechanism is testing whether trie addresses resolve as vm0+offset+slide versus cache-relative or text-VM-relative forms, plus reading cache header fields at offsets 0x10, 0x14, 0x90, and 0x98. The output shows whether each candidate is mapped and what bytes appear there, letting the researcher pick the correct trie base computation.
research/option-b/cal.c: cal.c scans arm64 Mach-O headers mapped by the APE loader in memory to find libsystem_kernel and libsystem_pthread dylibs and print their header address, __TEXT vmaddr, and exports trie offset. It walks load commands LC_SEGMENT_64 (0x19), LC_ID_DYLIB (0xd), and LC_DYLD_EXPORTS_TRIE (0x80000033), probing pages from 0x180000000 to 0x1a0000000 with syscall 78 (mincore) at +/-0x4000 steps. It reports hdr=, textvm=, trie_off=, and id= for any matching dylib without asserting a pass/fail result.
research/option-b/cal2.c: cal2.c probes a running macOS process by dlsym-ing libc/libpthread functions (getpid, pthread_self, sysctl, mmap) and iterating shared-cache images (_dyld_image_count, _dyld_get_image_name, _dyld_get_image_header, _dyld_get_image_vmaddr_slide) for libsystem_kernel, libsystem_pthread, libsystem_c.dylib, and libdyld. For each matching image it walks the Mach-O load commands printing LC_SEGMENT_64 fields (segname, vmaddr, fileoff, vmsize) and LC_DYLD_INFO_ONLY/LC_DYLD_EXPORTS_TRIE export trie offsets (dataoff, datasize, runtime pointer = header+dataoff). Its purpose is to map where libsystem export tries and segments live at runtime so an option-b resolver can compute libsystem function addresses from the shared cache.
research/option-b/call0.c: This test checks whether a zero-import binary can call libSystem by calling dlsym at a hardcoded same-machine address 0x188910c04, then resolving getpid and malloc and invoking getpid via the returned pointer.
research/option-b/caltrie.c: This program tests candidate formulas for locating the shared cache trie and resolving "getpid" in a macOS arm64 APE loader study, using _dyld_get_image_header on libsystem_kernel plus load-command scan for LC_DYLD_EXPORTS_TRIE (0x80000033) / LC_DYLD_INFO_ONLY (0x80000022) to grab dataoff. It walks the trie with uleb decoding and a mincore-based mapped() check (SYS_mincore on 16KB-aligned pages), skipping stub entries (flags bit 0x08), then tests six candidate trie addresses including hdr+off, slide+off, cachebase 0x188830000+off, and 0x1ff05c000+slide+(off-0x4000) against expected getpid 0x188cde178. The output format labels which candidate matches with , though the file itself does not state the actual result.
research/option-b/caltrie2.c: caltrie2.c probes the macOS shared cache's trie export table by testing four candidate base+offset addresses (kernel header+0xb686d0, text vmaddr, cache base, ld vmaddr minus ld file offset) and checking mincore mapping plus first bytes. It walks the trie with ULEB128 decoding of terminal size, child count, edge labels, child offsets, and export flags, skipping stub-and-resolver entries (flag 0x08), to look up "getpid" and compare against expected 0x188cde178. The file prints results but does not state which candidate matched.
research/option-b/cb.c: Scans memory from 0x190000000 downward in 0x4000 steps, using mincore to locate the dyld shared cache base by matching the 0x646c7964 ("dyld") header magic, then prints the cache base, getpid address, and libSystem handle. It relies on syscall SYS_mincore and the no-cache mincore flag 0x80 to skip unmapped pages, probing dyld cache layout on macOS arm64 for an APE loader. It showed nothing itself beyond the printed addresses, serving as a cache-base discovery probe.
research/option-b/cdump.c: This C program dumps the macOS arm64 dyld shared cache header mapped at a hardcoded base of 0x188830000 to inspect its magic, mapping and image-text offsets, platform, and format version. It walks mappingCount mapping structs (address/size/fileOffset/maxProt/initProt) and the first 6 imgtext entries with paths, reading fields at the cache's mappingOffset and imagesTextOffset without any syscall.
research/option-b/cdump2.c: cdump2.c is a macOS arm64 research probe that reads a dyld shared cache header mapped at the hardcoded base 0x188830000 and dumps its fields (magic, mappingOffset/Count, imagesOffset/Count, dyldBase, uuid, cacheType, branchPools, accelerateInfo, imagesText offset/count, platform, formatVersion) plus the first 40 mapping entries (addr, size, fileOff, maxProt/initProt at 32-byte strides) and first 5 text images (addr and path via pathFileOffset at +24). It uses fixed struct offsets in the dyld_cache_header (mapping info at +16/+20, images at +24/+28, dyldBase at +32, uuid at +88/+96, cacheType at +104, branchPools at +112/+116, accelerateInfo at +120/+128, imagesText at +136/+144, platform at +216, formatVersion at +220) and reads mapping entries with 32-byte size and image text entries with pathFileOffset at offset 24. The file only prints observed values and makes no claim about what was shown.
research/option-b/classify.c: This static probe classifies libSystem functions on arm64 macOS as direct-svc syscall wrappers, mach-svc wrappers, or real C code by disassembling the first 12 instructions of each dlsym'd symbol. It scans for the svc #0x80 encoding 0xd4001001 (syscall within first 6 insns), and for a movn-x16 (0x92800000 mask) at insn 0 or 1 indicating a mach trap. The file states no results; it only prints a table of symbol, kind, and raw first four instruction words.
research/option-b/ctxdump.c: ctxdump.c probes four hardcoded arm64e userspace addresses (0x188a39022, 0x18cd01780, 0x18d1b2092, 0x18d1c665f) by printing ASCII and hex dumps of memory around each pointer, plus bytes preceding it. It reads raw memory through direct pointer dereferences (no syscalls despite including sys/syscall.h), dumping 16-24 bytes per site to inspect what lies at those cached addresses. The file does not state what it discovered.
research/option-b/decode.py: A stdin filter that decodes 32-bit hex AArch64 instruction words into mnemonics, handling MOVN/MOVZ/MOVK (masks 0xff800000 base 0x92800000/0xd2800000/0xf2800000 with hw, imm, rd fields), SVC #0x80 (0xd4001001), RET (0xd65f03c0), and generic SVC (0xd4000001 masked). It is a pure disassembly helper for inspecting little-endian instruction encodings and makes no claims about results.
research/option-b/diag.c: diag.c is a diagnostic probe that prints the saved link register and stack pointer of an APE loader entry, then scans the stack for saved return addresses falling within loaded dyld image ranges (using LC_SEGMENT_64 vmaddr/vmsize offsets). It also runs mincore probes on candidate addresses to map which are resident. It tests whether the loader can resolve entry state and page residency without direct mach_vm_region traps.
research/option-b/dsym.txt: A dyld symbol dump for a macOS arm64 APE loader listing resolved addresses of libc/libSystem/pthread/dispatch functions such as mmap, mprotect, pthread_jit_write_protect_np, sys_icache_invalidate, posix_spawn-adjacent names, and semaphores; it is research reference data, not an executable test, and reports no pass/fail result.
research/option-b/entrycap.s: A macOS arm64 assembly entry-capture stub for an APE loader that saves the incoming x30 link register and stack pointer to globals _ent_x30 and _ent_sp before branching to _main, using ADRP/ADD page-relative addressing and no syscalls; it reports no result itself.
research/option-b/findid.c: This diagnostic dumps the Mach-O header and load commands of the mapped libSystem at fixed address 0x198b70000 by reading them via its own rs3 syscall 78, printing segment, dylib id, dyld exports trie, and symtab fields, then hexdumps 48 bytes of a presumed exports trie at 0x190340000+0x10e6488; it uses magic ncmds at offset 32 and LC constants (0x19, 0xd, 0x80000022, 0x80000033, 0x2) to walk commands. It is purely a reconnaissance probe and reports no results itself.
research/option-b/findtable.c: A research probe that scans the macOS dyld shared cache header (base 0x188830000) for the real cache image-offset/entry-count table by looking for plausible (offset,count) pairs and validating entry path strings starting with '/'. It uses SYS_mincore with the 0x80 bit to test whether pages at a candidate offset are resident/mapped. It prints candidate tables and how many of the first five entries have valid absolute paths, serving as a heuristic to locate the cache's image table in memory.
research/option-b/forkchk.c: forkchk.c tests raw BSD syscall 2 (fork) under a macOS arm64 APE loader by calling getpid (syscall 20) before and after fork and checking getppid (syscall 39), printing the raw x0 return, p0/p1 and a child flag.
research/option-b/forkchk2.c: forkchk2.c is a tiny macOS arm64 test that calls SYS_fork via syscall() and prints parent/child pids, ppid, and the raw fork return value. Its key mechanism is the direct SYS_fork syscall number rather than libc fork(), checking whether fork works in the APE loader context. It prints p0, p1, ppid, fork_x0, and is_child, but records no asserted result.
research/option-b/forkchk3.c: forkchk3.c is a minimal macOS test that calls getpid and fork to check whether a forked child reports a new pid and its parent via getppid. Its only mechanism is the fork libc call, with no syscall numbers, Mach-O offsets, load commands, or flags involved. The file prints p0, p1, ppid, fork_rc, and is_child but states no result.
research/option-b/hedge/hres.c: This test builds a zero-import binary that weakly references a hedge dylib and, after renaming libSystem so it never loads, scans the shared cache export tries via syscall 78 (mprotect-based mapped() probing) to resolve _malloc and _getpid, then tries calling them. It parses Mach-O headers, LC_DYLD_INFO/LC_DYLD_EXPORTS_TRIE (0x80000033) load commands, and walks compressed export tries with prefix pruning in the 0x180000000-0x1c0000000 cache range. It demonstrates whether symbols remain resolvable in the cache without libc loaded, testing if getpid works but malloc (which needs libc state) succeeds only if a hedge dylib provides libc.
research/option-b/hedge/libhedge.c: A trivial stub defining hedge_anchor returning 7, likely a placeholder object for the option-b hedge loader experiment; it tests no syscalls, offsets, load commands, or flags and reports no results.
research/option-b/hedge/main.c: A minimal C program for the macOS arm64 APE loader research that simply returns exit code 42; it has no syscalls, offsets, load commands, or flags and reports no findings.
research/option-b/hedge/rs.h: rs.h declares rs_syscall6 and inline wrappers rs0..rs6/mt0/mt6 that OR syscall numbers with the macOS BSD class 0x2000000 (Mach traps 0x1000000) for arm64 syscalls, plus minimal write/puts/putn/puth output helpers.
research/option-b/hedge/rs_syscall.s: This assembly file provides _rs_syscall6, a BSD/Mach syscall wrapper for macOS arm64, taking a class-tagged number in x0 (0x2000000 for BSD, 0x1000000 for mach), moving args into x0-x5, issuing svc #0x80 on x16, and negating x0 into -errno on carry. It is a hedge mechanism for an APE loader's Option B path, converting Darwin's carry-flag error convention to the Linux negative-errno convention. The file states no observed result.
research/option-b/imgtable.c: Probes the dyld shared cache header at a fixed base 0x188830000 to dump imagesTextOffset/imagesTextCount at +136/+144 and the 32-byte imagesText entries (three qwords plus pfo 32-bit path offset), then reads imagesOffsetOld/count at +24/+28 and prints old 32-bit image offsets as path strings.
research/option-b/imgtest.c: This probe reads an in-memory dyld shared cache at base 0x188830000, decoding imagesTextOffset/imagesTextCount from the header at offsets +0x88/+0x90 and per-image 32-byte entries {uuid[16], loadAddress u64, textSegmentSize u32, pathOffset u32}. It maps image base+pathOffset with SYS_mincore (syscall number, 16384-byte page probe) checking the 0x80 incore bit to validate the pointer before printing up to 70 chars of path. It tests whether the hardcoded cache header/entry layout yields readable image paths on macOS arm64.
research/option-b/logs/BUILD.md: This file documents the clang/ld build recipes for a freestanding arm64 macOS APE loader, using -ffreestanding with nm -u as a check that no libc symbols leak in and an assembly rs_syscall.o for raw syscalls. It details the zfinal resolver (prefix pruning plus cache image table), a drop-in dlsym, and a weak/strong link matrix on stub dylibs showing single or double absent weak links still SIGABRT while a weak absent plus strong real -lSystem runs, plus install-name renaming that requires re-signing. It also covers an @loader_path/libhedge.dylib hedge build, size/otool LC_ and minos checks, and per-minos sweep builds from 10.15 through 26.0.
research/option-b/logs/class_scan.txt: This log scans x16 class numbers passed to a macOS arm64 syscall scheme, recording each rc to map valid classnum handling. It shows x16=2 returning rc=50915 and x16=20 returning rc=50969 with duplicate classnum=2 output, while many classes return negative errno-like values such as -14, -78, and -1. The scan also records exit=140 for x16=101 and x16=9999 and large rc values for classnum=0x2000014 and 0x1000014.
research/option-b/logs/raw_all.txt: This macOS arm64 log runs a syscall probe suite for an option-b APE loader, invoking each syscall by number (e.g. getentropy 500, pipe 42, fork 2, issetugid 327, thread_selfid 372, sigaltstack 53, sysctl 202, getrlimit 194, select 93, pread 153, sigaction 46, getpid 20, mach_task_self 0x100001c). Most calls returned rc=0 or valid values, but nanosleep:101 hit SIGNAL=12 and mach_task_self 0x100001c returned rc=-14.
research/option-b/mapdump.c: This file dumps 64 raw 8-byte qwords starting at offset 0x228 from hardcoded base address 0x188830000 as a quick memory inspection utility. It uses direct pointer dereferencing with no syscalls, load commands, or flags. It prints each qword with its offset but records no analysis of the values.
research/option-b/min.c: A minimal probe in the option-b APE loader research that calls rs1(1,0) after printing hi. The rs1 syscall wrapper (likely raw ARM64 syscall with number in x16 or similar) is invoked with args 1 and 0, testing a single syscall path. Just prints hi and performs one syscall; no results documented.
research/option-b/mt.c: mt.c probes three negative macOS arm64 syscall numbers (-55, -51, -53) via rs_syscall6 and prints raw return values to test task_self_trap, mach_reply_port, and thread_self_trap mappings. The source comments only document the movn x16 encodings; it reports no observed results.
research/option-b/mt2.c: mt2.c tests Mach trap calls from APE code by passing negative x16 syscall numbers derived from libSystem stubs (task_self_trap=-28, host_self_trap=-29, thread_self_trap=-27, mach_reply_port=-26) through rs_syscall6, printing the returned port names. It verifies that negative x16 values encode Mach traps and that all calls return normally with stable port numbers.
research/option-b/mt3.c: research/option-b/mt3.c tests whether Mach trap returns in an arm64 APE loader follow the BSD syscall carry-flag convention, by calling the task_self_trap Mach trap number -28 four times via a raw svc #0x80 path and printing x0 without consulting carry; it concludes that Mach traps return kern_return_t in x0 and must not apply the carry/negate convention used for BSD syscalls.
research/option-b/mydlsym.c: A zero-import dlsym replacement for macOS arm64 that locates the dyld shared cache via the dylib magic 0x646c7964 in the 0x180000000-0x1c0000000 range, parses Mach-O export tries (ULEB128, LC_SEGMENT_64, LC_DYLD_INFO/LC_DYLD_EXPORTS 0x80000022/0x80000033), and resolves symbols by walking the trie using only the syscall rs3 (78, mach_vm_region) for page mapping checks. It tests resolution of getpid, malloc, pthread_* and calls the resolved getpid/pthread_self.
research/option-b/off2.c: This C program prints a hardcoded dyld-style symbol offset table for 55 libSystem functions (fork, pipe, mmap, pthread_*, dispatch_*, sem_*, dlopen, sysctl, etc.) with magic 0, version 4, and sizeof 440, emitting each name paired with offset 8+8*i starting at 8. It is meant to generate or validate the offsets for an APE loader's libSystem export table on macOS arm64. The file does not state any observed result.
research/option-b/offsets.c: This C program prints the byte offsets of each field in a mirrored Go syslib struct (2 int32 fields then uintptr fields) to verify ABI layout between C and Go for a macOS arm64 APE loader. It uses offsetof on magic, version, and ~50 function pointer slots covering pthreads, memory, syscalls, semaphores, dlopen, and sysctl; it prints only, not asserts, so it reveals layout but states no confirmed result.
research/option-b/p_a.c: p_a.c is a simple ABI test that calls rs1 with syscall number 42 on macOS arm64, passing a two-int array and printing the return value and both output fds via o_lbl/o_putn, building on rs.h. It checks whether the pipe syscall succeeds through the raw ABI and what fds it returns, preceding and following the call with volatile dummy loops and labeled trace output.
research/option-b/p_apple.c: This research helper dumps argc, a few argv entries, envc, and 40 raw stack words above the envp terminator to hunt for Apple's auxiliary vector and the dyld main() apple/4th vector on macOS arm64. Its mechanism is a stack-scan heuristic (envp walk plus fixed-size word dump with a pointer-range sanity check), not a naked x3 trampoline or syscall-numbered path. It only prints observations and reaches no stated conclusion about load-command or flag offsets.
research/option-b/p_apple2.c: Dumps the first 16 qwords at the _apple_vec symbol, printing each value and probing plausible pointers for a dereferenced first word or 8 printable ASCII bytes; no syscalls or load commands involved.
research/option-b/p_b.c: p_b.c is a minimal APE test that calls rs2 with syscall number 101 (SYS_nanosleep on Darwin arm64) via the loader's syscall wrapper, printing labels before and after. It tests whether an indirect syscall through the option-b loader returns cleanly and reports the return code. The file shows no recorded result, only the intended before/after markers and rc output.
research/option-b/p_class.c: p_class.c is a tiny macOS arm64 test in an APE loader research project that reads a decimal syscall class number from argv[1] and invokes rs_syscall6 with that number and five zero argument registers. Its key mechanism is passing the classnum directly as the syscall number to rs_syscall6 with no x16/class routing; it prints the classnum hex and signed return code but records no conclusion.
research/option-b/p_comm.c: This file probes the macOS arm64 commpage at fixed address 0xFFFFFC000, dumping its first 24 magic words and scanning 1024 8-byte slots for pointers into the 0x180000000-0x1c0000000 shared-cache range or above 0x100000000, using only o_lbl/o_puth output helpers with no syscalls or load commands. It is a reconnaissance test to locate kernel-published pointers or shared cache references inside the commpage for APE loader research. The file records no results, only labels the scan as done.
research/option-b/p_fork.c: p_fork.c tests fork/wait behavior from an APE payload by issuing raw syscalls via rs0/rs1/rs4 wrappers. It calls fork (2), getpid (20) from the child, exit (1), and wait4 (7) in the parent, printing the return code and status. The file records only output labels, not explicit conclusions.
research/option-b/p_fork2.c: This test calls getpid (syscall 20) and fork (syscall 2) via the rs0/rs1 raw-syscall helpers to check whether fork works in the option-b APE loader. It prints parent and child PIDs so the child can be distinguished, with the child exiting via exit (syscall 1); the source does not state the observed result.
research/option-b/p_fork3.c: p_fork3.c is a small APE test that invokes raw syscalls via rs0/rs1 wrappers: pipe (42), fork (2), and twice getppid (39), plus getpid (20), to print pid, ppid, fork return code, and a global g=111 via o_lbl/o_putn. It has no explicit arm64/macOS loader offsets or load-command flags, relying on rs.h. The file contains no stated result beyond an "ambiguous" comment; it just prints state and sets g=222.
research/option-b/p_fork4.c: This test calls raw syscalls fork (2), getpid (20), wait4 (7), pipe (42), and exit (1) to check forked-child behavior under an APE loader, identifying the child either by fork returning 0 or by a changed PID. It reports the child PID, parent wait4 result, raw status, WIFEXITED, and exit code via o_putn/o_puth helpers.
research/option-b/p_fork5.c: p_fork5.c calls rs0() with x86_64 syscall numbers 20 (getpid), 2 (fork), and 39 (getppid) to compare parent PID, forked child PID, and parent-of-parent PID. It prints who= plus P, M, PP, and forkrc values to test whether fork returns correctly under an APE loader. The file does not state a result.
research/option-b/p_fork_libc.c: A tiny libc fork test that calls fork(), prints pid/ppid/forkrc and global g, sets g=222, and has the parent wait(0) for the child. It exercises fork/wait/getpid/getppid via libc with no syscall numbers, offsets, load commands, or flags, and records no stated result.
research/option-b/p_fork_syscall.c: This file tests whether the raw syscall(SYS_fork) interface works on macOS arm64 by forking and checking return values and PIDs, using syscall numbers from sys/syscall.h and wait4 with a numeric status pointer instead of waitpid. It distinguishes child detection via rc==0 versus via PID change from the original getpid.
research/option-b/p_lr.c: This Option-B test prints the captured entry x30(LR) and sp, then scans up to 0x8000 bytes of the stack for 8-byte words in the 0x180000000..0x200000000 address range to find saved pointers, capping output at 40 hits and reporting the total.
research/option-b/p_machstub.c: This macOS arm64 probe dumps the first 5 words of exported libSystem stubs for mach traps and libc functions, decoding arm64 MOVZ/MOVK x16 immediate loads and SVC into their syscall numbers to reveal trap constants and load-command style stubs. It checks whether symbols like task_self_trap, mach_msg_trap, mmap, and pthread_jit_write_protect_np resolve and what immediate each stub materializes before svc. The file contains no reported results.
research/option-b/p_mark.c: A macOS arm64 APE test that installs a SIGSEGV handler via sigaction (syscall 46) and faults at address 0 to verify the handler runs. The naked handler uses the raw syscall base x16=0x80 with errno-free numbers (write=4, exit=1) to print "HANDLER" and exit with status 0x5e.
research/option-b/p_min.c: This test probes mincore(2) via syscall 78 on macOS arm64 to map page residency at candidate addresses and walk 16K pages downward from 0x188963000, flagging unmapped regions and checking for the Mach-O magic 0xfeedfacf. It uses rs3 for the syscall and prints raw rc plus vec[0] bytes, but the file contains no results or conclusions.
research/option-b/p_neg.c: The file p_neg.c tests negative syscall numbers in the option-b APE loader by parsing a signed integer argv[1] and passing it to rs_syscall6 with zeroed args, printing the x16 value and return code. Its mechanism is the rs_syscall6 wrapper, which presumably issues an svc with the given number in x16. The file contains no recorded result, so it only provides the test harness without stating what was shown.
research/option-b/p_qsa.c: p_qsa.c probes sigaction (syscall 46) queries for SIGBUS (10) and SIGSEGV (11) on macOS arm64 via rs3, passing a local struct with handler, mask, and flags fields pre-filled with 0xdead sentinels to detect kernel writes. It prints return codes and the handler/mask/flags values to check whether the syscall succeeds and whether oldact is fully populated. The file does not state a conclusion, only the observed values.
research/option-b/p_scan.c: Scans the macOS shared cache region 0x180000000-0x200000000 in 0x4000 steps using mincore (syscall 78) to skip unmapped pages, then checks for Mach-O magic 0xfeedfacf headers and parses load commands (LC_SEGMENT_64 0x19, LC_ID_DYLIB 0xd, LC_DYLD_INFO_ONLY 0x80000022, LC_DYLD_EXPORTS_TRIE 0x80000033) to print the image id, ASLR slide (base minus __TEXT vmaddr), and export trie offsets, plus getpid via syscall 20. It prints libSystem/libdyld and the first few images found and a running total of Mach-O headers discovered.
research/option-b/p_sig.c: This test installs SIGBUS/SIGSEGV handlers via sigaction syscall 46 and then dereferences address 0x100000000 to force a fault. The handler records the signal and calls exit(1) via syscall 1, because a normal return needs a kernel trampoline. Output shows sigaction return codes and whether delivery reached the handler or the process survived.
research/option-b/p_sig2.c: p_sig2.c tests SA_SIGINFO signal handler delivery on a macOS arm64 APE loader by calling sigaction (BSD syscall 46, sig 10) with flags=0 to install a handler, then deliberately dereferencing unmapped address 0x100000000 to trigger SIGSEGV/SIGBUS. The handler increments a hit counter and directly writes "HR!\n" via write (syscall 4, fd 2) to prove kernel-to-handler delivery without relying on sigreturn state restoration. It showed no inline results in the file itself beyond printing install status, faulting message, returned value, and hit count.
research/option-b/p_sig3.c: This test installs a SIGBUS handler via sigaction (syscall 46) with flags=0 (no SA_SIGINFO) and then dereferences address 0x100000000 to trigger the fault. The handler sets g_n to the signal number and exits with code sig+100, proving delivery.
research/option-b/p_sig4.c: This test installs a SIGBUS handler via sigaction syscall 46 on Darwin arm64 and then dereferences unmapped address 0x100000000 to trigger a fault. The naked handler increments g_hits and exits with raw syscall 1 (exit) using status 0x5a, avoiding any libc or unwinder dependency. If the crash is delivered as SIGBUS rather than SIGSEGV, the handler runs and the process exits 0x5a instead of printing "returned v=".
research/option-b/p_sig5.c: The file p_sig5.c tests whether a raw signal handler is invoked when installed without libc, using a static hit counter and rs1(1,0x5b). It explores the hypothesis that the kernel needs a libc trampoline (SA_ flags or restorer) for handlers to fire, but the main body only emits a log label and returns, so no sigaction setup or result is actually performed.
research/option-b/p_sig6.c: This file tests installing a SA_SIGINFO (flag 0x40) SIGBUS handler via rt_sigaction (syscall 46) and faulting at address 0x100000000. Its naked handler bumps a counter and exits via syscall 1 (exit) with code 0x5b=91, printing the install result and hit count.
research/option-b/p_sig_libc.c: This test checks whether libc sigaction with a POSIX handler catches SIGBUS in an APE program, unlike raw signal syscalls. It installs h for SIGBUS on a fault caused by reading address 0x100000000, which should hit the handler and _exit 0x5b. It does not state the observed result here.
research/option-b/p_soft.c: Tests whether a SIGUSR1 signal handler can be installed and invoked with soft sigaction in a macOS arm64 APE (using rs3=syscall 46 sigaction, rs2=37 kill, rs0=20 getpid, rs1=1 exit) and a naked handler that increments g_hits then exits via svc 0x80 syscall 0x5d; it prints the syscall return codes and final hit count to show if delivery occurred.
research/option-b/p_stubs2.c: This diagnostic dumps the first six words of each Mach trap stub in libSystem on macOS arm64, resolving symbols by name via dlsym so their opcode or syscall-number encoding can be inspected. It covers trap names like task_self_trap, mach_msg_trap, semaphore variants, swtch/thread_switch, kernelrpc vm and port traps, and mach_vm plus task_threads functions. The file itself records no observed output or conclusion.
research/option-b/p_vec.c: p_vec.c is a freestanding Mach-O header probe that scans the initial stack and then walks pages downward from the entry LR page using mincore (syscall 78) to find Mach-O images. It parses mach_header_64 (magic feedfacf/feedface, ncmds sane) and dumps load commands LC_SEGMENT_64 0x19, LC_SYMTAB 0x2, LC_DYSYMTAB 0xb, LC_DYLD_INFO_ONLY 0x80000022, LC_DYLD_CHAINED_FIXUPS 0x80000034, LC_DYLD_EXPORTS_TRIE 0x80000033, printing segment vmaddr/vmsize and symtab offsets. It prints LR/sp and each stack or page hit but reports no actual findings besides the dumps.
research/option-b/p_z.c: p_z.c installs a SIGSEGV handler via sigaction (syscall 46, SA_SIGINFO flag 0x40) and then dereferences a NULL pointer to test whether the APE loader handles the fault; the handler increments a counter and exits via write(1,...) syscall 0x5c (92) rather than returning. It tests whether the process survives the null read, printing "reading *0 ..." and either "survived x=..." or nothing if the handler exits.
research/option-b/p_zero.c: This test installs a SIGSEGV handler (sigaction syscall 46, flags 0x40) whose naked handler records a hit and exits via syscall 1 with status 0x5c, then dereferences address 0 to confirm the handler fires on arm64 macOS.
research/option-b/probe1.c: probe1.c is a basic identity test for the macOS arm64 APE loader, issuing raw BSD syscalls via rs0/rs2/rs6 to check getpid(20), getuid(24), getppid(39), geteuid(25), getgid(47), getegid(43), issetugid(147), getentropy(500), and sysctl(202) with MIB {1,3} for ncpu and {6,3} for kern.hostname. It prints each result via o_putn/o_puts helpers. The file does not state its observed results.
research/option-b/probe2.c: probe2.c is a macOS arm64 syscall probe exercising mmap/mprotect/munmap (197/74/73), open/pread (5/153), getrlimit/setrlimit (194/195), pselect (394), sigaltstack/sigaction/sigprocmask (53/46/48), gettimeofday (116), and an invalid class-1 syscall 9999, printing return values and outgoing struct fields. It tests that the sandboxed loader correctly handles these BSD syscalls with proper MAP_ANON|MAP_PRIVATE and PROT flags, and that unknown class-1 syscalls return -78 ENOSYS rather than trapping.
research/option-b/probe3.c: Probe3.c is a macOS arm64 syscall-surface probe for the APE loader that exercises pselect, pipe, fork, read/write, wait4, nanosleep, clock_gettime (113), getentropy (500), issetugid (327), thread_selfid (372), sem_open (268), sysctl (202) with mib {6,3} and {1,10}, and Mach traps 3/28/29 via raw syscall helpers rs0-rs6/mt0. It tests Darwin BSD syscall numbers and Mach trap numbers directly without libc wrappers, expecting clock_gettime(113) to return negative ENOSYS since Darwin has no such BSD syscall. The file does not record observed outputs, only prints results.
research/option-b/probe_final.c: A tiny probe that invokes BSD syscall 101 (nanosleep) with SYS_rs1 and no argument, labeled as an argc/argv/envp-bearing test of the 0-arg path and printing the return code plus unreachable markers. It exists to confirm that a syscall from the loaded process gets killed by SIGSYS at the expected point, so execution never reaches the code after the call. The file itself states the expectation via its "(SIGSYS expected)" and "UNREACHED" labels.
research/option-b/probe_final2.c: This probe tests whether several Darwin syscalls work under a raw syscall path on macOS arm64, including select(93) with a zeroed 50us timeout as a nanosleep substitute, thread_selfid(372), and __semwait_signal(334). It uses raw syscall wrappers rs0, rs5, and rs6, and left a stub mentioning swtch_pri as Mach trap 0x7500000 via x16=-89, but the file shows no recorded results.
research/option-b/probe_pipe.c: probe_pipe.c is a small APE test that calls BSD syscalls 42 (pipe) and 101 (nanosleep) via rs1/rs2 to probe macOS arm64 syscall numbers and pipe fd behavior. It prints the pipe return value and read/write fds plus the nanosleep result, but records no explicit findings.
research/option-b/probesafe.c: probesafe.c tests whether specific high arm64 addresses are readable by forking a child per address and reading one byte, then checks residency via mincore syscall on a 16384-byte page-aligned range. It probes addresses like 0x100000000, 0x188830000, and 0x1f4bec1a0, printing FAULT/ok, the byte, mincore return, and vec0. The file reports no conclusions beyond the per-address success/fault and mincore output.
research/option-b/pts.c: This file is a small probe that prints the value of mach_task_self() in a macOS arm64 APE loader research context. Its only mechanism is the mach_task_self() call plus printf, with no syscall numbers, offsets, load commands, or flags involved. It reports no result beyond the printed task port value.
research/option-b/raw_all.c: This macOS arm64 APE research file tests which Syslib entries can be replaced by direct raw syscalls, using hand-coded BSD syscall numbers (e.g. getentropy 500, issetugid 327, thread_selfid 372, sigaltstack 53, getrlimit 194, pread 153, select 93, nanosleep 101) via rs0/rs1/rs2/rs3/rs5/rs6 and a Mach trap mt0(28) for task_self, run one test per invocation since SIGSYS kills the process.
research/option-b/resolved.txt: A macOS arm64 dyld shared-cache symbol resolution dump from the option-b zero-import resolver, listing each libSystem symbol's resolved runtime address (cachebase 0x188830000) with fork, sigaction, raise left unresolved as 0x0.
research/option-b/resolver.c: This experimental macOS arm64 APE loader file scans dyld shared-cache trie nodes in mapped memory to resolve exports like "dlsym" by walking ULEB128-encoded terminal-size, flags, and child offsets, using mincore (SYS_mincore) for mapping checks. It probes three candidate cache slides (0x8830000, 0x8828000, 0x88b0000) at ldvm+slide+(off-ldfo) with ldfo=0x4000 and off=0xb3cce0, printing raw bytes and the resolved address, and reportedly expected a match at 0x188910c04. It thus tests slide and offset determination for manual shared-cache export lookup without dyld.
research/option-b/rootbytes.c: A research probe that tests whether computed dyld trie-node addresses are readable in the current process by mincore-mapping each candidate and dumping 20 bytes. It uses SYS_mincore (BSD syscall) on page-aligned addresses derived from a hardcoded slide 0x8830000 plus dyld shared cache VM base 0x1ff05c000 minus LINKEDIT offset 0x4000 plus four offsets (0xb3cce0, 0xb686d0, 0xb71498, 0xb4dc50), printing mapped status and hex/ASCII content. The file does not state which probes succeeded.
research/option-b/rs.h: rs.h is a header of macOS arm64 syscall wrappers for the option-b APE loader research: rs_syscall6 plus inline rs0..rs6 (class 0x2000000 for BSD syscalls) and mt0/mt6 (class 0x1000000 for mach traps). It adds raw write/puts/putn/puth output helpers built on the BSD write syscall (number 4), used for dependency-free diagnostics. The file reports no results itself.
research/option-b/rs_syscall.s: This file is a hand-written arm64 assembly shim, rs_syscall6, that implements a Linux-style 6-argument syscall wrapper for a macOS APE loader research project. It moves the class-tagged syscall number from x0 into x16 (0x2000000|n for BSD, 0x1000000|n for mach), shifts args x1..x6 into x0..x5, executes svc #0x80, and negates x0 on carry to normalize macOS error returns to -errno. No test results are stated in the file.
research/option-b/scancall.c: scancall.c is a small macOS arm64 research helper that takes a syscall number N from argv, invokes it with zero arguments via the rs0() wrapper in rs.h, prints the raw return code, and exits. Its key mechanism is dynamically passing an arbitrary BSD syscall number to rs0 for direct probing of syscall behavior in the APE loader context. It encodes no fixed syscall numbers, offsets, load commands, or flags, and reports no conclusions itself.
research/option-b/scanguard.c: scanguard scans BSD syscall numbers LO..HI by forking per number, running rs0(N) in the child, and printing the return code. The parent uses rs4(7,pid,&st,1,0) wait4 with WNOHANG in a spin guard, kills hangs with rs2(37,pid,9) kill SIGKILL, and decodes status low 7 bits for signals. It reports HANG, WAITERR, or SIGNAL per syscall but has no recorded results in the file.
research/option-b/sl.c: Tests SIGSEGV signal handling under a macOS arm64 APE loader by installing a sigaction handler for SIGSEGV, then dereferencing a near-null pointer to trigger the fault. It prints install status and expects the handler to run, writing "HANDLER RAN" and exiting with 0x5b, verifying the loader properly delivers signals to the guest.
research/option-b/sl2.c: sl2.c tests whether a SIGSEGV handler can be installed via raw syscall(SYS_sigaction, SIGSEGV, ...) on macOS arm64, then deliberately dereferences a null pointer to trigger a fault. It uses the raw SYS_sigaction syscall number instead of libc signal()/sigaction() and checks sizeof(struct sigaction). The file does not state the observed result.
research/option-b/sl3.c: sl3.c tests Darwin arm64 sigaction with a 24-byte kernel struct (handler, mask, flags, tramp) via SYS_sigaction for SIGSEGV, then triggers a null deref; it prints the syscall return code and the handler writes HANDLER-RAN and exits 0x5b.
research/option-b/stub.c: A minimal C stub returning 42, used as a test payload in a macOS arm64 APE loader research project. It has no syscalls, offsets, load commands, or flags, and reports no findings.
research/option-b/stubdylib.c: A two-function C stub for building a macOS arm64 stub dylib used in an APE loader research project, providing dummy symbols for dylib construction and link experiments.
research/option-b/stubs/libMacos.dylib.tbd: A stub TAPI .tbd file for arm64 macOS defining libMacos.B.dylib with a single exported symbol _dummy, used as a placeholder dylib in the APE loader's Option B stub set. It contains only target, install-name, and export metadata, no syscall numbers, offsets, load commands, or flags, and reports no experimental results.
research/option-b/stubs/libMacos.tbd: This is a TAPI .tbd stub defining a dummy arm64 macOS dylib named /usr/lib/libMacos.B.dylib exporting only _dummy, likely used as a placeholder link target. It declares tbd-version 4, target arm64-macos, current-version 1, with one symbol but no load commands, syscalls, offsets, or results.
research/option-b/stubs/libNope.tbd: A TAPI .tbd stub declaring arm64-macos target and install-name /usr/lib/libNope.1.dylib, exporting only symbol _dummy2 with current-version 1. It tests the APE loader's ability to parse tbd-version 4 text stubs and resolve a minimal exported symbol. No runtime result or syscall details are stated in the file.
research/option-b/sub2.c: Scans 0x180000000-0x2c0000000 in 0x4000 steps for dyld_v1 shared cache headers, using SYS_mincore to skip unmapped pages and printing magic, mappingOffset, mappingCount, and per-mapping addr/size/fileOff. It is a read-only mapping probe, not a loader test, and reports total headers found (caps at >8). No result is stated.
research/option-b/sub3.c: Tests candidate virtual-address formulas for locating a dyld shared-cache trie/data offset (dataoff 0xb686d0) relative to an __LINKEDIT header at 0x207888000 and its 0x1ff05c000 VM map; probes mappedness via SYS_mincore and dumps 16 bytes per candidate. Candidates are ld_vm+(dataoff-ldfo), the 4K map 0x1ff058000+dataoff, and ld_vm+dataoff, with a planned libdyld cross-check (hdr 0x18890f000, dataoff 0xb3cce0 expecting 0x188910c04) not actually executed.
research/option-b/subc.c: Scans the 0x180000000-0x1c0000000 region in 4 KiB steps using SYS_mincore to test page residency, then prints dyld_v1 subcache headers, locating shared cache mappings for the APE loader.
research/option-b/symtab.c: This test parses libsystem_kernel's Mach-O header at fixed address 0x188cdd000 to locate the LC_SYMTAB command (cmd 0x2) and print its symoff, nsyms, stroff, and strsize. It forks child processes to safely read the symbol table entries and string table across possible fault boundaries, then dumps the first 12 nlist64 entries (16 bytes each) with their names. The file shows the mechanism for extracting symbol/string offsets from a shared cache-resident dylib header in the APE loader research, but reports no explicit success/failure conclusion.
research/option-b/t1.c: This test uses raw Darwin arm64 syscalls via svc #0x80 with numbers placed in x8 and x16 (0x2000000|n) to probe write(4), getpid(20), and mmap(197) from a potential APE-loaded process, printing argc, argv0, and syscall return values before returning 7.
research/option-b/tbase.c: tbase.c probes dyld's exports trie by trying three candidate base addresses (dyld shared cache base plus data offset 0xb3cce0, Mach-O header 0x18890f000 plus offset, and text vmaddr 0x190340000 plus offset), validating each page with SYS_mincore before parsing the trie with ULEB128 terminators, child offsets, and edge strings to look up "dlsym". It uses a bounded recursive walk (300-char prefix, 800000-step cap) and checks the export flags bit 0x08 (stub-and-resolver) before reading the offset. No result is stated; it prints found/off/nodes/steps and compares the resolved address to the expected 0x188910c04.
research/option-b/tform.c: This scratch C tool parses macOS (arm64) dyld export tries from hardcoded addresses (libdyld, kernel, pthread, libsystem_c) using vmaddr slide and dataoff minus the 0x4000 file offset difference, validating reads with SYS_mincore. It implements a ULEB128-based trie walker that follows terminal and edge-offset records, skipping stub-and-resolver exports via flag 0x08, and reads child edges from null-terminated strings. Running it reproduced dlsym, getpid, pthread_self, and sysctl symbol addresses with MATCH against the wanted offsets.
research/option-b/tramp1.c: On macOS arm64, tramp1.c queries the kernel's SIGSEGV sigaction via SYS_sigaction to inspect the returned handler, mask, flags, and tramp fields, then prints sizeof(struct sigaction). The key mechanism is syscall(SYS_sigaction, SIGSEGV, 0, &k) using a custom 4-field struct whose trailing tramp pointer follows handler, mask, and flags. It is a probe of sigaction layout/offsets for the APE loader's signal-trampoline handling, with no stated result beyond the printed output.
research/option-b/trie.c: Scans Mach-O images in the shared cache from 0x180000000 to 0x200000000 stepping 0x4000, validating headers via magic 0xfeedfacf and parsing LC_SEGMENT_64, LC_ID_DYLIB, LC_DYLD_INFO_ONLY (0x80000022) and LC_DYLD_EXPORTS_TRIE (0x80000033) to locate the __TEXT vmaddr and export trie offset. It walks the dyld export trie with ULEB128 decoding and a recursive prefix buffer to find the "getpid" symbol, trying both (export_off + slide) and (image_base + export_off) as the trie address. The code is incomplete exploratory scaffolding (a broken iterative trie_lookup remains unused); it prints each image's id, trie_off, and resolved symbol address, then reports the count of scanned images.
research/option-b/trie2.c: Scans arm64 macOS user addresses for the shared cache's libSystem dylib Mach-O header (magic 0xfeedfacf via syscall 78), parses load commands LC_SEGMENT_64/LC_ID_DYLIB/LC_DYLD_INFO_ONLY/LC_DYLD_EXPORTS_TRIE/LC_SYMTAB to extract __TEXT vmaddr, __LINKEDIT vmaddr/fileoff, exports trie offset/size, and symtab info. It tests four candidate trie base-address computations (header+off, TEXT vmaddr+off, LINKEDIT vm + (off-fileoff), dsc+off) by reading mapped pages and dumping the root terminalSize/childCount bytes.
research/option-b/trie3.c: trie3.c scans 0x180000000-0x200000000 in 0x4000 steps using syscall 78 with VM_PROT_READ|VM_PROT_EXECUTE (16384) to validate Mach-O headers (magic 0xfeedfacf) and locate /usr/lib/system/libdyld.dylib via LC_ID_DYLIB, parsing LC_SEGMENT_64, LC_DYLD_EXPORTS_TRIE, LC_DYLD_INFO_ONLY and LC_SYMTAB. It then tests four candidate addresses (base+off, vmaddr+off, ldvm+(off-ldfo), dsc+off) for the exports trie by reading the root ULEB terminalSize, printing slide, __LINKEDIT, trie, symtab info and first four bytes of each candidate.
research/option-b/trie_ok.c: This file tests whether it can hand-parse dyld export tries from fixed in-memory addresses by walking trie nodes via the mincore syscall (SYS_mincore, 0x1) to safely read only mapped pages. It decodes ULEB128 terminal flags and child offsets, skipping nodes with the 0x08 stub flag, and looks up symbol names like dlsym, getpid, mmap, pthread_self, sysctl, and sysctlbyname. It reported MATCHes for dlsym=0x188910c04, getpid=0x188cde178, pthread_self=0x188d1d590, and sysctl=0x188bb5944, confirming the hand-rolled trie walker against expected header offsets.
research/option-b/trie_probe.c: This probe hardcodes a trie address (lt=0x18890f000+0xb3cce0) and uses mincore via SYS_mincore to test whether that page is mapped and resident. If mapped it dumps the first 32 bytes as hex/ASCII and manually decodes the trie root node's leading ULEB128 terminalSize plus the following childCount byte. The file shows no recorded result, only the probe logic and printouts.
research/option-b/triefind.c: Scans mapped memory in 0x180000000-0x220000000 on macOS arm64 via SYS_mincore to find export-trie edge byte patterns for symbols dlsym, getpid, sysctl, pthread_self (length byte, name bytes, then uleb child offset). Uses 0x4000 page granularity and the mincore 0x80 bit to test if pages are resident/mapped. Prints up to 12 raw byte hits per symbol with addresses for relating to Mach-O headers.
research/option-b/trieloc.c: Probes a hardcoded libdyld Mach-O header at 0x18890f000 in a macOS arm64 process, parsing LC_SYMTAB (cmd 0x2) and LC_DYLD_INFO(_ONLY) (0x80000033/0x80000022) load commands for symoff/nsyms/stroff/strsize and dataoff about the dyld data. Uses fork-based safe reads and mincore plus a list of candidate base/slide addresses (slide 0x8830000, bases 0x180000000, 0x1ff05c000, 0x188830000) to test which address lands in the mapped Mach-O and where the symbol table is readable. Outputs per-candidate read=ok/FAULT and hex bytes plus up to 4 symtab entries' strx, but the file records no conclusion on which candidate succeeded.
research/option-b/trydump.c: This tool probes the macOS shared-cache dyld symbol trie by using syscall SYS_mincore with page-aligned addresses to test mappedness over a 0x180000000-0x260000000 scan range and dumps 24 bytes at each candidate address, testing two address formulas (slid base plus unslid offset minus 0x4000 file offset, and slid base plus raw offset) around the LINKEDIT subcache base 0x1ff05c000 with slide 0x8830000 and offsets like 0xb686d0, plus a raw page scan for the 'dlsym' substring to locate cached symbol strings.
research/option-b/tw.c: Walks macOS arm64 kernel exported-symbol tries at hardcoded addresses (kernel hdr+0xb686d0, pthread hdr+0xb71498, sysctl hdr+0xb4dc50) to resolve names like getpid, mmap, pthread_self, sysctl. Uses rs3 syscall 78 (mprotect) with 0x3fff page-alignment to probe mapped pages, uleb128 decoding of trie nodes (terminal flag, 0x08 stub flag, child count, name, child offset). Prints resolved addresses with expected ground-truth values for verification.
research/option-b/verify.c: verify.c dlsym-probes about 55 libc/libSystem symbols (pthreads, mmap, semaphores, sysctl, JIT write-protect, dispatch) via RTLD_DEFAULT and prints each name with its resolved address. It tests which APIs an APE-loaded binary can resolve on macOS arm64; the file reports no pass/fail result itself.
research/option-b/w2.c: Tiny C test program whose main returns 42, used as a minimal payload for macOS arm64 APE loader research; it involves no syscalls, offsets, load commands, or flags and reports no findings itself.
research/option-b/w3.c: w3.c is a minimal macOS test program that returns 42 from main, used to probe exit-status handling in the option-b APE loader. It contains no syscalls, offsets, load commands, or flags and reports no results itself.
research/option-b/w4.c: w4.c is a minimal macOS APE test stub whose main returns 42, used to probe or validate loader behavior for a trivial program. It contains no syscalls, offsets, load commands, or flags itself beyond the return value. No result is recorded in the file.
research/option-b/w5.c: w5.c is a minimal macOS arm64 test program that just returns 42. It exists to check a loader's handling of a trivial Mach-O executable with no syscalls, offsets, or load commands of interest. The file itself shows no further findings.
research/option-b/where.c: This macOS arm64 test program probes dyld image layout by printing the vmaddr slide and header of image 0 and the first six loaded images, then dlopen's libSystem with RTLD_NOLOAD and dlsym's getpid from RTLD_DEFAULT. It uses no syscall numbers, offsets, or load commands directly, relying only on _dyld_get_image_* APIs and _NSGetArgv. The file records no observed results, only the printed diagnostics.
research/option-b/wl.c: wl.c is a minimal C stub whose main returns exit code 42, used to produce an arm64 Mach-O binary for testing APE loading on macOS. It contains no syscalls, offsets, load commands, or flags of its own, so it tests only whether a loader can map and run a trivial Mach-O and report its exit status.
research/option-b/wsearch.c: wsearch.c scans the mapped 0x180000000-0x1c0000000 address range page-by-page using mincore (SYS_mincore) to test residency, searching each 16K page for the byte string "getpid" and printing up to 40 hits with addresses. It relies on the mincore syscall plus a 0x3fff page mask and 0x4000 stride. It reports hits= counts but states no conclusion.
research/option-b/x.s: x.s defines a global myvar quad and _getmv, which computes myvar's runtime address via adrp plus add with @PAGE/@PAGEOFF relocations before returning in x0. It tests or supports PC-relative addressing of data in a macOS arm64 APE loader context, with no syscalls, load commands, or flags present. The file records no observed result.
research/option-b/zboot.c: This research file implements an Option B zero-import bootstrap that finds libSystem's Mach-O images by scanning mapped memory from 0x180000000 to 0x260000000 using mincore (syscall 78) with bit 0x80 indicating unmapped pages, then resolves exported symbols by walking the LC_DYLD_EXPORTS_TRIE (0x80000033) or LC_DYLD_INFO_ONLY (0x80000022) export trie parsing ULEB128 values to compute image_header+offset addresses without any dlsym or LC_SYMTAB use. It locates images via LC_ID_DYLIB (0xd) install-name matching and demonstrates resolving and successfully calling _getpid from libsystem_kernel.dylib.
research/option-b/zfinal.c: A macOS arm64 zero-import symbol resolver test that finds libSystem images in the dyld shared cache image table via mincore (syscall 78) and resolves exported symbols by walking the LC_DYLD_EXPORTS_TRIE/LC_DYLD_INFO_ONLY export trie at __LINKEDIT.vmaddr+slide+(dataoff-fileoff), with runtime symbol addresses computed as image_header+export_offset (image-relative, no slide). It locates images by matching LC_ID_DYLIB install names, computes slide as runtime_header minus __TEXT.vmaddr, and scans cache bases from 0x180000000 to 0x1c0000000 in 0x4000 steps for the "dyld" magic. It parses the cache image table at offsets 0x88 (table offset) and 0x90 (count), tests ~53 symbols across libsystem_kernel, pthread, platform, c, libdyld, and dispatch, and explicitly disables the cross-image fallback (primary-route test only) with no printed pass/fail summary.
research/option-b/zr2.c: zr2.c is a macOS arm64 research harness that walks an in-memory dyld export trie to resolve symbols by name, reading bytes defensively via mincore page-mapping checks and ULEB128 decoding. It handles trie node format (terminal flags, reexport ordinals, edge strings, child offsets) against fixed test addresses for libdyld, libsystem_kernel, libsystem_pthread and libsystem_c, comparing found offsets to expected values. The file does not state a final pass/fail result in its source.
research/option-b/zres2.c: This file tests a zero-import dyld export-trie resolver on macOS arm64 by locating in-memory Mach-O images via LC_ID_DYLIB and walking their LC_DYLD_EXPORTS_TRIE/LC_DYLD_INFO_ONLY data to resolve symbols for four target dylibs. It uses syscall 78 (mach_vm_region via rs3) for mapping checks, scans 0x180000000-0x260000000 in 0x4000 steps, and parses LC_ID_DYLIB (0xd), LC_DYLD_EXPORTS_TRIE (0x80000033), and LC_DYLD_INFO_ONLY (0x80000022) plus ULEB128 trie nodes and __TEXT segment (0x19) validation. It prints header and resolved addresses but the source does not state observed results.
research/option-b/zres3.c: zres3.c is a zero-import dyld symbol resolver for macOS arm64 that locates the shared cache at 0x180000000, finds images via the cache's imagesText table at offset 0x88/0x90, and walks each image's LC_DYLD_EXPORTS_TRIE (0x80000033) or LC_DYLD_INFO_ONLY (0x80000022) trie to resolve symbols, using page-mapped checks via syscall 78 (mach_vm_region) and page flags. It tests resolution of 11 symbols such as _getpid, _mmap, _pthread_self, _dlsym, and _sys_icache_invalidate, printing each resolved address. The file records no explicit result.
research/option-b/zres4.c: This macOS arm64 APE research file walks the dyld export trie of a mapped libsystem_kernel image to resolve the export "_getpid", using syscall 78 (mincore) via rs3 to test page residency before reading, and parsing LC_DYLD_EXPORTS_TRIE/LC_DYLD_INFO_ONLY load commands (0x80000033/0x80000022) to find dataoff and the trie base at 0x1898456d0; it prints dataoff, trie address, mapped status, root bytes, and the found offset plus computed address.
research/option-b/zres_final.c: A zero-import macOS arm64 symbol resolver test that scans the shared cache (0x180000000-0x260000000, 0x4000 stride) for Mach-O image starts, matches LC_ID_DYLIB names, and walks LC_DYLD_EXPORTS_TRIE/LC_DYLD_INFO_ONLY tries to resolve symbols. It uses rs3 syscall 78 for page mapping checks. It resolves 11 libsystem symbols (_getpid, _mmap, _pthread_self, _dlsym, etc.) and prints their addresses or "image not found".
research/option-b/zresolve.c: A macOS arm64 research harness that parses dyld's per-image trie export tables using mincore to validate mapped memory, reading ULEB-encoded trie nodes at a fixed shared-cache slide (0x8830000) and dyld cache offsets. It resolves symbols like _dlsym, _getpid, _mmap, _pthread_self, _sysctl from hardcoded libdyld/libsystem trie offsets, checks REEXPORT (0x08) terminal flags, and compares results to expected addresses (want fields). It reports MATCH/MISMATCH per symbol, validating the trie-walking approach.
research/option-b/zsym.c: zsym.c implements zero-import symbol resolution for a macOS arm64 APE loader by scanning mapped Mach-O images from 0x180000000 to 0x220000000, finding LC_ID_DYLIB by install name, locating the LC_DYLD_EXPORTS_TRIE or LC_DYLD_INFO_ONLY trie, and walking ULEB128 trie nodes to resolve a symbol offset. It uses rs3 syscall 78 for page permission checks, checks magic 0xfeedfacf, and skips stub/resolver entries via trie flags bit 0x08. main verifies results against recorded dlsym answers (getpid 0x188cde178, mmap 0x188cde96c, pthread_self 0x188d1d590, sysctl 0x188bb5944).
